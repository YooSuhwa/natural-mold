## Codebase Patterns
- Backend: FastAPI + SQLAlchemy async + Alembic
- AI Runtime: LangChain 1.x + LangGraph 1.x, executor.py에서 create_react_agent 사용 중
- 도구 타입: builtin, prebuilt, custom, mcp, skill_package
- MCP: httpx 직접 구현 (mcp_client.py), StructuredTool 수동 래핑 (tool_factory.py)
- 패키지 매니저: uv (backend), pnpm (frontend)
- 린트: ruff (line-length=100, target=py312)
- 테스트: pytest + aiosqlite in-memory
- DB: PostgreSQL 16 (docker-compose)

## M1 목표
- create_react_agent → create_deep_agent (deepagents 패키지)
- MCP 직접 구현 → langchain-mcp-adapters
- SPEC.md 참조

## Gotchas
- executor.py build_agent()에 create_agent + create_react_agent 폴백 패턴 있음
- tool_factory.py에 MCP + builtin + prebuilt + custom 도구 생성 함수 혼재
- mcp_client.py에 test_mcp_connection()/list_mcp_tools()는 유지해야 함 (UI에서 사용)
- chat_service.py build_tools_config()에 MCP 이름 가공 로직 있음

## M1 삭제 분석 결과 (2026-04-05)
- create_mcp_tool, _build_args_schema, call_mcp_tool, _extract_text는 외부 참조가 executor.py lazy import뿐. 테스트 직접 참조 없음 → 안전 삭제 가능
- MCP 이름 가공 로직(chat_service.py:201-214)은 외부 테스트 없음 → 안전 삭제 가능
- build_agent() 재작성 시 test_executor.py 2개 테스트도 재작성 필요
- TokenTrackingCallback는 프로덕션 코드에서 미사용 (테스트만 존재) → M4 판단
- skill_tool_factory.py, skill_executor.py는 M3 scope → M1에서 건드리지 않음

## S2 아키텍처 리서치 결과 (2026-04-05)

### SPEC 대비 실제 API 차이점
- `create_deep_agent(skills=[...])` → skills 파라미터 없음. backend=CompositeBackend(routes={"/skills/": FilesystemBackend(...)}) 사용
- `create_deep_agent(memory=[...])` → memory 파라미터 없음. backend CompositeBackend + store=PostgresStore(...) 조합
- `middleware` 파라미터는 확인됨: `middleware: Sequence[AgentMiddleware] = ()`
- 반환 타입 CompiledStateGraph — 기존 astream() 호환

### MCP 어댑터 패턴
- MultiServerMCPClient가 주 인터페이스: {"server_name": {"transport": "http", "url": "...", "headers": {...}}}
- get_tools() → list[BaseTool] 직접 반환 (수동 래핑 불필요)
- auth_config → HTTP headers 변환 필요 (api_key → Authorization, jwt_token → Bearer)

### ADR-001 핵심 설계
- build_agent() → create_deep_agent 단일 호출 (폴백 제거)
- MCP 도구: 서버 URL별 그룹화 → MultiServerMCPClient → get_tools() → 이름 필터링
- builtin/prebuilt/custom 도구 생성은 기존 tool_factory.py 유지
- streaming.py는 변경 없음 (astream API 동일)

## S3 완료 (2026-04-05) — Jensen
- executor.py: create_react_agent → create_deep_agent (deepagents 0.4.12)
- MCP 도구: create_mcp_tool → langchain-mcp-adapters (MultiServerMCPClient)
- ADR 대비 차이: transport는 "streamable_http" (ADR에서는 "http"로 기술됨)
- create_deep_agent에 skills/memory 파라미터 존재 (ADR 리서치 시점과 달리 0.4.x에서 추가됨)
- MultiServerMCPClient.get_tools()는 async context manager 없이 호출 가능 (매 tool call마다 새 세션)

## S4 완료 (2026-04-05) — Jensen
- tool_factory.py: _build_args_schema(), create_mcp_tool() 제거 (51줄 감소)
- mcp_client.py: _extract_text(), call_mcp_tool() 제거 (17줄 감소)
- chat_service.py: MCP 이름 중복 감지/리네이밍 로직 제거
- test_executor.py: build_agent 테스트 2개를 create_deep_agent mock으로 수정
- ruff + grep 검증 통과: 제거된 함수 참조 0건

## S5 통합 검증 결과 (2026-04-05) — Bezos
- ruff check: 통과 (All checks passed)
- pytest: 314 passed, 9 failed
- M1 관련 테스트 109개: 전부 통과
- 제거된 함수 참조 grep: 0건 (clean)
- 9개 실패는 모두 pre-existing (main에서도 동일하게 실패, git stash로 확인):
  - test_streaming.py 3건: 미들웨어 JSON 필터(character-by-character 스트리밍) 도입 후 테스트 미수정 (commit 1760bc0)
  - test_skill_package.py 6건: skill_tool_factory 도구 이름 변경 후 테스트 미수정 (commit 1760bc0)
- 결론: M1 변경으로 인한 regression 없음

## M2 삭제 분석 결과 (2026-04-06) — Bezos

### 핵심 발견
- save_message() 안에 auto-title 로직(L92-102)이 숨어 있음. 단순 삭제 불가 → 별도 함수로 추출 필요.
- token_usages.message_id FK가 messages 테이블에 의존 → messages DROP 전에 FK 변경 필수 (순서 위반 시 마이그레이션 실패)
- trigger_executor.py가 save_message 2곳 호출 — M4 scope(deep agent invoke 교체)이지만 M2 save_message 제거 시 동시 수정 필요
- Message 모델은 테스트 5개 파일에서 참조 (test_conversations_router, test_trigger_executor, test_usage_service, test_usage_router, test_chat_service). 테스트 수정 범위 넓음.
- 프론트엔드 Message 타입(frontend/src/lib/types/index.ts:168)은 백엔드 MessageResponse와 1:1 대응. 백엔드 응답 형태를 최대한 유지해야 프론트엔드 변경 최소화 가능.
- Conversation.messages relationship은 코드에서 직접 접근 없음 (cascade 설정만). 안전 삭제 가능.

### 삭제 순서 제약
1. auto-title 추출 → 2. token_usages FK 변경 → 3. router/trigger 수정 → 4. service 정리 → 5. Message 모델 제거 → 6. messages 테이블 DROP

## S2 아키텍처 설계 결과 (2026-04-06) — Pichai

### Checkpointer 초기화 패턴
- 모듈-레벨 싱글턴: `agent_runtime/checkpointer.py` (init/shutdown/get/delete_thread)
- psycopg_pool.AsyncConnectionPool + AsyncPostgresSaver
- main.py lifespan에서 초기화/정리
- Connection string: `database_url.replace("+asyncpg", "")` (psycopg v3 형식)
- 의존성: `langgraph-checkpoint-postgres` 추가 필요

### 메시지 변환 패턴
- checkpointer.aget_tuple() → checkpoint["channel_values"]["messages"] → list[BaseMessage]
- message_utils.py에 `langchain_messages_to_response()` 추가 (기존 convert 함수와 대칭)
- created_at: 합성 타임스탬프 (conversation.created_at + idx ms) — 프론트엔드 변경 제로

### 핵심 흐름 변경
- messages_history: 전체 히스토리 → 새 메시지만 (checkpointer가 히스토리 자동 관리)
- save_message() 호출 불필요 (checkpointer 자동 저장)
- list_messages() 호출 불필요 (checkpointer 자동 복원)
- auto-title: save_message()에서 분리 → maybe_set_auto_title() 독립 함수

### 대화 삭제 패턴
- delete_thread(): checkpointer의 pool로 직접 SQL 삭제 (checkpoints, checkpoint_blobs, checkpoint_writes)
- 순서: checkpoint 삭제 → conversations 테이블 삭제

### DB 마이그레이션
- messages 테이블 제거
- token_usages: message_id FK → conversation_id FK
- Alembic 순서: conversation_id 추가 → message_id 제거 → messages DROP

### 주의사항
- build_agent()는 매 요청마다 호출됨 — 에이전트 캐싱은 향후 최적화 대상
- aget_tuple()의 내부 구조(channel_values)는 LangGraph 버전에 따라 변경 가능 — message_utils.py로 캡슐화
- trigger_executor.py도 save_message 호출 중 — M4 scope지만 M2 save_message 제거 시 동시 수정 필요 (S1 분석 결과)

## M2-S3 완료 (2026-04-06) — Jensen
- checkpointer.py: AsyncPostgresSaver 싱글턴 (init/shutdown/get/delete_thread)
- main.py lifespan: checkpointer 초기화(시드 후) + 종료(스케줄러 후)
- executor.py: build_agent()에 checkpointer=get_checkpointer() 전달
- langgraph-checkpoint-postgres는 이미 pyproject.toml에 존재 (M1에서 추가됨)
- conn string 변환: database_url.replace("+asyncpg", "") — psycopg v3 형식

## M2-S4 완료 (2026-04-06) — Jensen
- message_utils.py: langchain_messages_to_response() 추가 (BaseMessage → MessageResponse 변환)
- chat_service.py: save_message()/list_messages() 삭제, maybe_set_auto_title() 추가
- conversations.py: GET messages → checkpointer.aget_tuple(), POST messages → 새 메시지만 전달 + auto-title, DELETE → delete_thread() 추가
- trigger_executor.py: save_message 호출 2곳 제거 (checkpointer 자동 저장)
- trigger_executor는 title="자동 실행: ..." 으로 생성하므로 maybe_set_auto_title 불필요
- save_token_usage의 message_id → conversation_id 변경은 S5에서 모델 변경과 함께 처리

## M2-S5 완료 (2026-04-06) — Jensen
- Message 클래스 제거 (conversation.py), Conversation.messages relationship 제거
- TokenUsage: message_id FK → conversation_id FK 변경
- models/__init__.py: Message 제거
- schemas/conversation.py: MessageResponse에서 from_attributes 제거 (더 이상 ORM 아님)
- chat_service.save_token_usage: message_id → conversation_id 파라미터 변경
- Alembic 마이그레이션: conversation_id 추가 → 데이터 마이그레이션 → message_id 삭제 → messages DROP
- checkpointer.py: psycopg lazy import (TYPE_CHECKING 패턴) — 테스트 환경(aiosqlite)에서 psycopg 미설치 문제 해결
- main.py, executor.py, conversations.py: checkpointer import를 함수 내부로 이동 (lazy)
- 테스트 5개 파일 수정: Message 참조 제거, checkpointer mock 사용, conversation_id FK 반영
- 결과: ruff 통과, 321 passed (기존 대비 regression 없음)

## Gotchas (M2)
- psycopg 라이브러리는 테스트 환경에서 사용 불가 (libpq 미설치). checkpointer 관련 import는 반드시 lazy로.
- patch 경로: lazy import 시 `app.agent_runtime.checkpointer.get_checkpointer`로 패치해야 함 (모듈 원본)

## M2-S6 통합 검증 결과 (2026-04-06) — Bezos
- ruff check: 전체 통과
- pytest: 321 passed, 0 failed (M1 대비 321 → 321, 기존 pre-existing 9건 수정됨)
- S1 삭제 분석 체크리스트 12/12 항목 전부 PASS
- Message ORM 참조 0건, save_message 참조 0건, list_messages 참조 0건, message_id 참조(app/) 0건
- 신규 코드 4개 함수 존재 확인: init/shutdown/get_checkpointer, delete_thread, langchain_messages_to_response, maybe_set_auto_title
- Alembic 마이그레이션: 올바른 순서 (conv_id 추가 → 데이터 마이그레이션 → message_id 삭제 → messages DROP → NOT NULL)
- 결론: M2 regression 없음, 게이트 통과

## M3 삭제 분석 결과 (2026-04-06) — Bezos

### 핵심 발견
- skill_tool_factory.py + skill_executor.py → 전체 삭제 가능 (외부 참조: executor.py lazy import 1건만)
- chat_service.py: get_agent_skill_contents() 삭제, build_effective_prompt() 단순화, build_tools_config() skill_package 블록 제거
- executor.py: skill_package elif 분기(L189-199) 제거
- config.py: skill_script_timeout, skill_max_output_bytes 제거 가능. skill_storage_dir/skill_max_package_bytes/conversation_output_dir는 유지 필수
- AgentSkillLink/skill_links는 유지 필수 (S3에서 skills 경로 매핑에 사용)
- 테스트 영향: test_skill_package.py의 TestSkillExecutor(7건)/TestSkillToolFactory(6건) 삭제, TestPromptInjection 수정

### 삭제 순서 제약
1. executor.py skill_package 분기 제거 → 2. chat_service.py 3개 함수 정리 → 3. skill_tool_factory.py 삭제 → 4. skill_executor.py 삭제 → 5. config/pyproject.toml/테스트 정리

## M3-S2 아키텍처 설계 결과 (2026-04-06) — Pichai

### Backend 선택: FilesystemBackend
- root_dir="./data", virtual_mode=True
- CompositeBackend 불필요 (스크래치패드 미사용)
- StoreBackend 불필요 (PoC에서 cross-thread 메모리 불필요)
- 심볼릭 링크 불가: FilesystemBackend는 O_NOFOLLOW로 symlink 차단

### Skills 경로 매핑
- skills=["/skills/"] → _list_skills가 data/skills/ 하위 서브디렉토리 자동 탐색
- _list_skills 동작: ls_info(source_path) → is_dir 필터 → {dir}/SKILL.md 다운로드 → frontmatter 파싱
- **주의**: source_path는 **스킬 컬렉션 디렉토리** (부모). 개별 스킬 디렉토리 직접 전달 불가
- Per-agent 필터링 없음 (PoC). 모든 에이전트가 /skills/ 전체 탐색. 프로그레시브 디스클로저로 보완.

### Memory 경로 패턴
- memory=["/agents/{agent_id}/AGENTS.md"]
- MemoryMiddleware: download_files() → file_not_found 시 무시 (에러 없음)
- data/agents/{agent_id}/ 디렉토리는 에이전트 생성 시 생성

### Text 스킬 처리
- text 스킬은 DB content 필드에만 존재 → 디스크에 SKILL.md로 물질화 필요
- skill_service에서 생성/수정 시 _materialize_skill_to_disk() 호출
- DB content = source of truth, SKILL.md = 파생물

### build_agent() 변경
- skills: list[str] | None, memory: list[str] | None 파라미터 추가
- create_deep_agent에 그대로 전달
- create_deep_agent 내부: SkillsMiddleware(backend, sources), MemoryMiddleware(backend, sources) 자동 생성

### 제거 대상 확인 (S4용)
- skill_tool_factory.py, skill_executor.py 전체 삭제
- build_effective_prompt() 스킬 주입 로직
- build_tools_config() skill_package 블록
- executor.py skill_package elif 분기

## M3-S3 완료 (2026-04-06) — Jensen
- executor.py: build_agent()에 skills/memory 파라미터 추가, create_deep_agent에 전달
- executor.py: execute_agent_stream()에 agent_skills/agent_id 파라미터 추가
- executor.py: FilesystemBackend(root_dir=_DATA_DIR, virtual_mode=True) 생성
- executor.py: skills_sources=["/skills/"] (agent_skills 있을 때), memory_sources=["/agents/{id}/AGENTS.md"] (agent_id 있을 때)
- executor.py: skill_package elif 분기 제거 (L189-199)
- executor.py: agent_id 전달 시 data/agents/{agent_id}/ 디렉토리 lazy 생성
- conversations.py: agent_skills 구성 (skill_links에서 storage_path 있는 것만) + execute_agent_stream에 전달
- trigger_executor.py: 동일하게 agent_skills + agent_id 전달
- test_executor.py: build_agent skills/memory 테스트 추가, execute_agent_stream skills/memory 전달 테스트 2건 추가
- _DATA_DIR 모듈 레벨 상수: ruff ASYNC240 (async 함수 내 Path.resolve) 회피
- 결과: ruff 통과, 14 passed (기존 12 + 신규 2)

## M3-S4 완료 (2026-04-06) — Jensen
- skill_tool_factory.py 삭제 (124줄)
- skill_executor.py 삭제 (93줄)
- chat_service.py: get_agent_skill_contents() 삭제, build_effective_prompt() → return agent.system_prompt, build_tools_config() skill_package 블록 제거, Path/settings import 제거
- config.py: skill_script_timeout, skill_max_output_bytes 제거
- pyproject.toml: ruff per-file-ignores 2줄 제거
- test_skill_package.py: TestSkillExecutor(7건)/TestSkillToolFactory(6건) 삭제, TestPromptInjection 수정 (7건→3건)
- grep 검증: "skill_package"(도구 타입), skill_tool_factory, skill_executor, create_skill_tools, execute_skill_script, SkillScriptResult, get_agent_skill_contents, skill_script_timeout, skill_max_output_bytes → 전부 0건
- 결과: ruff 통과, 29 passed (executor 14 + skill_package 15)

## Gotchas (M3)
- FilesystemBackend import는 lazy (함수 내부) — psycopg 패턴과 동일하지만, deepagents는 테스트 환경에서 설치 가능하므로 문제 없음
- ruff ASYNC240: async 함수 내에서 Path.resolve() 사용 금지 → 모듈 레벨 상수로 이동

## M3-S5 통합 검증 결과 (2026-04-06) — Bezos
- ruff check: 전체 통과
- pytest: 308 passed, 0 failed (M2 321 → M3 308, 감소분 13건은 삭제된 테스트: TestSkillExecutor 7 + TestSkillToolFactory 6)
- 삭제 체크리스트: 18/18 PASS
  - skill_package grep 2건은 upload_skill_package 함수명 (유지 대상) — false positive, 정상
- 유지 항목: 전부 PASS (AgentSkillLink 13refs, skill_links 14refs, skill_service.py/skills.py 존재, config 설정 유지)
- 신규 코드: PASS (executor.py skills/memory 파라미터, FilesystemBackend, conversations.py/trigger_executor.py agent_skills/agent_id 전달)
- 결론: M3 regression 없음, 게이트 통과

## M4 삭제 분석 결과 (2026-04-06) — Bezos

### 핵심 발견
- creation_agent.py: raw model.ainvoke() 사용. 단일 함수(run_creation_conversation), 서비스 레이어(agent_creation_service)에서만 호출. 시그니처/반환 형태 유지하면서 내부만 deep agent로 교체 가능.
- trigger_executor.py: execute_agent_stream() SSE 소비 → build_agent() + invoke() 직접 호출로 교체. SSE 파싱 11줄 제거. chat_service 헬퍼 5개는 전부 유지.
- streaming.py 미들웨어 필터: 삭제 가능. LLMToolSelectorMiddleware는 내부적으로 structured output 소비, 스트림에 노출 안 됨. deepagents PatchToolCallsMiddleware는 다른 문제(dangling tool call) 해결. 필터는 실질적으로 트리거되지 않는 방어 코드.
- middleware_registry.py PatchedLLMToolSelectorMiddleware: 유지 필요. deepagents가 const 정규화를 내부 처리하지 않음. langchain LLMToolSelectorMiddleware는 string 기대. GPT-4o + llm_tool_selector 조합 시 패치 없으면 깨짐.
- 시드 데이터: M1-M3 이후 불일치 없음. Template.recommended_tools 타입 힌트만 경미한 이슈 (dict → list).
- Dead code: TokenTrackingCallback만 미사용 (token_tracker.py). 나머지 코드베이스 깨끗.

### 테스트 영향 요약
- test_creation_agent.py: 10건 재작성 (mock 대상 변경)
- test_agent_creation_extended.py: 14건 patch 경로 수정
- test_trigger_executor.py: 7건 재작성 (mock 대상 변경), 4건 유지
- test_streaming.py: char-by-char 관련 assertion 제거

## M4-S2 아키텍처 설계 결과 (2026-04-06) — Pichai

### S1 분석 결과 수정
- **streaming.py 필터**: S1에서 "삭제 가능"으로 판단했으나, deepagents 소스 조사 결과 **유지 필요**로 수정.
  PatchToolCallsMiddleware는 `before_agent()` 훅만 구현 (dangling tool call 패치). 스트림 이벤트 필터링 없음.
  → LLMToolSelectorMiddleware의 `{"tools":[...]}` JSON이 스트림에 그대로 노출됨.

### 3가지 설계 결정

#### 1. creation_agent → create_deep_agent
- 전환하되 최소 변경: `build_agent(tools=[])` + `agent.ainvoke()`
- checkpointer 미사용 (DB JSON 히스토리 유지)
- JSON 파싱 로직 유지 (response_format은 프론트엔드 변경 필요 → M4 스코프 초과)
- 변경 파일: creation_agent.py만 (서비스 레이어 변경 없음)

#### 2. trigger_executor → direct invoke
- `_prepare_agent()` 추출 + `execute_agent_invoke()` 추가
- SSE 인코딩/디코딩 왕복 제거
- 반환: `str` (최종 content)
- 변경 파일: executor.py (리팩터), trigger_executor.py (단순화)

#### 3. streaming/middleware 정리
- **둘 다 유지** (S1 분석과 다름)
- streaming.py: PatchToolCallsMiddleware가 스트림 미필터링 → JSON 필터 필요
- PatchedLLMToolSelectorMiddleware: GPT-4o {"const"} 이슈 미수정 → 패치 필요
- 유일한 변경: 유지 사유 주석 추가

### 산출물
- docs/design-docs/adr-004-m4-cleanup.md 작성 완료
- docs/ARCHITECTURE.md M4 섹션 추가 완료

## Gotchas (M4)
- S1 삭제 분석과 S2 심층 조사가 streaming.py 필터에 대해 상반된 결론 도출. 심층 조사(실제 소스코드 확인)가 정확. 표면적 분석만으로 삭제 판단하지 말 것.
- create_deep_agent(tools=[])는 `tools or []` 내부 처리로 정상 동작. 빈 리스트 걱정 불필요.
- CompiledStateGraph는 invoke/ainvoke/stream/astream 모두 지원 (Runnable 인터페이스).

## M4-S3 완료 (2026-04-06) — Jensen
- executor.py: _prepare_agent() 추출 (도구/미들웨어/백엔드/스킬/메모리/체크포인터 빌드 공용화)
- executor.py: execute_agent_invoke() 추가 (ainvoke → 마지막 메시지 content 반환)
- executor.py: execute_agent_stream()을 _prepare_agent() + stream_agent_response()로 리팩터
- trigger_executor.py: execute_agent_stream SSE 파싱 → execute_agent_invoke 한 줄로 교체
- trigger_executor.py: json import 제거, full_content 변수 제거 (checkpointer 자동 저장)
- test_trigger_executor.py: mock_stream async generator → return_value/side_effect 교체
- 결과: ruff 통과, 24 passed (trigger 10 + executor 14)

## M4-S4 완료 (2026-04-06) — Jensen
- creation_agent.py: model.ainvoke() → build_agent(tools=[]) + agent.ainvoke() 전환
- creation_agent.py: system 메시지를 build_agent system_prompt로 분리 (messages에서 제외)
- test_creation_agent.py: mock 대상 create_chat_model → build_agent, ainvoke 반환 형태 변경
- test_creation_agent.py: system_prompt 검증은 mock_build.call_args.kwargs["system_prompt"]
- test_agent_creation_extended.py: 변경 없음 (서비스 레벨 mock으로 영향 없음)
- 결���: ruff 통과, 28 passed (creation 10 + extended 18)

## M4-S5 완료 (2026-04-06) ��� Jensen
- streaming.py: _is_tool_selector_json() + 버퍼링에 ADR-004 유지 사유 주석 추가
- middleware_registry.py: PatchedLLMToolSelectorMiddleware에 ADR-004 유지 사유 주석 추가
- 코드 로직 변경 없음
- 결과: ruff 통과, 11 passed

## M4-S6 완료 (2026-04-06) — Jensen
- token_tracker.py 삭제 (프로덕션 코드 참조 0건)
- test_token_tracker.py 삭제
- models/template.py: recommended_tools 타입 힌트 dict → list
- 결과: ruff 통과, 302 passed (M3 308 - 삭제된 테스트 6건 = 302)

## Gotchas (M4 구현)
- build_agent(model, tools=[], system_prompt=...) 호출 시 system_prompt는 keyword arg. 테스트에서 call_args[0][2]로 접근하면 IndexError. call_args.kwargs["system_prompt"] 사용.
- trigger_executor에서 full_content 변수가 ruff F841 (unused) 발생. checkpointer 자동 저장 덕에 로컬 변수 불필요.
- replace_all=true가 동일 문자열이 아닌 다른 줄에 있는 유사 패턴을 놓칠 수 있음. 수동 확인 필요.

## M4-S7 통합 검증 결과 (2026-04-06) — Bezos
- ruff check: 전체 통과
- pytest: 302 passed, 0 failed (M3 308 - 삭제된 테스트 6건 = 302)
- S1 삭제 체크리스트 18/18 PASS (S2 수정사항 반영):
  - token_tracker.py 삭제 완료 (파일+클래스 모두 grep 0건)
  - streaming.py 필터 유지 (S2 결정 준수, ADR-004 주석 2곳)
  - middleware_registry.py 패치 유지 (ADR-004 주석 1곳)
  - trigger_executor: execute_agent_stream→execute_agent_invoke, SSE 파싱 제거, json import 제거
  - creation_agent: model.ainvoke()→build_agent(tools=[]), 테스트 mock 변경
  - template.py 타입 힌트 수정 (dict→list)
- M1-M3 제거 항목 regression 0건:
  - create_mcp_tool/_build_args_schema/call_mcp_tool/_extract_text: 0건
  - skill_tool_factory/skill_executor/create_skill_tools/execute_skill_script: 0건
  - create_react_agent: 0건
  - save_message/list_messages 3건은 false positive (list_messages_from_checkpointer + 라우터 함수명)
- 신규 코드: _prepare_agent(), execute_agent_invoke() (executor.py), build_agent(tools=[]) (creation_agent.py) 확인
- 결론: M4 regression 없음, 게이트 통과

## M5 Provider 시스템 구현 (2026-04-06) — Jensen

### M5-Task1 (M1-S1): Backend 인프라 완료
- encryption.py: Fernet 기반 encrypt/decrypt, encryption_key 없으면 평문 패스스루
- LLMProvider ORM: llm_providers 테이블 (id, name, provider_type, base_url, api_key_encrypted, is_active, timestamps)
- models 테이블: provider_id FK, context_window, input_modalities, output_modalities 추가, model_name/display_name 200자로 확장
- Alembic migration: llm_providers 생성 + 기존 models → llm_providers 데이터 마이그레이션
- Provider CRUD API: GET/POST/PUT/DELETE /api/providers, POST /test, GET /discover-models
- model_discovery.py: OpenAI/Anthropic(정적)/Google/OpenRouter/OpenAI-compatible 지원
- model_metadata.py: 정적 메타 정보 (context_window, modalities, cost)
- default_providers seed: OpenAI, Anthropic, Google (API 키 없이)
- 테스트: 13건 신규 (providers 8 + encryption 2 + discovery 3)

### M5-Task2 (M2-S2): Models API + model_factory 완료
- schemas/model.py: provider_id, context_window, modalities, ModelBulkCreate 추가
- model_service.py: list_models/create_model → dict 반환 (provider_name 포함), bulk_create_models 추가
- model_factory.py: PROVIDER_MAP에 openrouter/openai_compatible 추가 (ChatOpenAI 사용)
- conversations.py: api_key를 llm_provider에서 decrypt하여 전달 (폴백: model-level key)
- trigger_executor.py: 동일하게 llm_provider 우선
- chat_service.py: selectinload(Agent.model).selectinload(Model.llm_provider) 추가
- models.py 라우터: /api/models/bulk POST 추가
- 결과: 315 passed, 0 failed (기존 302 + 신규 13)

### M5-Task3 (M3-S3): Frontend 전면 재구성 완료 — Zuckerberg
- types/index.ts: Provider/DiscoveredModel 타입 추가, Model에 provider_id/context_window/modalities 추가, ModelBulkCreateRequest 추가
- api/providers.ts (신규): Provider CRUD + test + discover API 클라이언트
- api/models.ts: bulkCreate 엔드포인트 추가
- hooks/use-providers.ts (신규): TanStack Query hooks 6개
- hooks/use-models.ts: useBulkCreateModels 추가
- components/model/provider-card.tsx (신규): Provider 카드 + getProviderIcon
- components/model/provider-form.tsx (신규): Provider 추가/수정 Dialog (inner component 패턴)
- components/model/model-add-dialog.tsx (신규): 3단계 모델 추가 (provider→discover→register/manual)
- components/model/model-select.tsx (신규): 검색 가능한 모델 선택기 + formatContextWindow
- app/models/page.tsx: 전면 재작성 (Tabs: Providers|Models)
- agents/[agentId]/settings/page.tsx: Select → ModelSelect 교체
- agent-node.tsx: provider 아이콘 + context_window 배지 추가
- messages/ko.json: provider 섹션 추가, model 섹션 확장
- 검증: pnpm build PASS + pnpm lint PASS

### Gotchas (M5)
- model_service에서 list_models/create_model 반환 타입이 ORM → dict로 변경됨. from_attributes=True 기반 테스트가 dict와도 호환됨 (Pydantic이 dict도 처리)
- provider_service.list_providers는 JOIN + group_by로 model_count 집계. SQLite (테스트)에서도 정상 동작.
- Alembic migration의 데이터 마이그레이션은 gen_random_uuid() 사용 (PostgreSQL 전용). SQLite에서는 실행 안 됨 (테스트에서는 create_all로 테이블 직접 생성).
- @base-ui/react Select onValueChange 시그니처: (string|null, eventDetails). setState 직접 전달 불가 → `(val) => { if (val) setState(val) }` 래핑 필요.
- React 19 lint: useEffect 내 동기 setState 금지. Dialog 폼 초기화는 inner component 패턴으로 해결.
- IS NOT DISTINCT FROM 구문은 PostgreSQL 전용. NULL 비교 안전 처리.

## M5 통합 검증 결과 (2026-04-06) — Bezos
- ruff check: 전체 통과
- pytest: 327 passed, 0 failed (M4 302 + M5 신규 13 + 검증 보강 12 = 327)
- pnpm build: 전체 통과 (타입 에러 0)
- 신규 파일 18/18 전부 존재 확인
- 테스트 보강:
  - test_providers.py: +5건 (/test, /discover-models, 404, model_count)
  - test_model_discovery.py: +5건 (test_connection success/auth/connect, google, openai_compatible)
  - test_models.py: +2건 (bulk_create, provider_id 연결)
- Alembic Migration 2 (models.api_key_encrypted/base_url DROP) 보류:
  - conversations.py에서 model-level api_key 폴백 사용 중 (L: llm_provider 우선, model key 폴백)
  - 폴백 코드 제거 없이 컬럼 DROP하면 기존 model-level key 설정이 깨짐
  - 완전 전환 후 DROP 권장
- 결론: M5 regression 없음, 게이트 통과

## M5 리뷰 이슈 수정 완료 (2026-04-06) — Jensen
- encryption.py: Fernet lazy 캐싱 + InvalidToken fallback + warning 로그
- providers.py: 전 엔드포인트에 get_current_user 의존성 주입
- provider_service.py: _UPDATABLE_FIELDS 화이트리스트, delete_provider tuple 반환, get_provider_with_count 추가
- model_service.py: encrypt_api_key 적용, provider_type 검증, bulk_create 중복 skip + None 반환
- conversations.py + trigger_executor.py: model-level key fallback에 decrypt_api_key 적용
- llm_provider.py schema: Literal 타입 + Field(max_length=100)
- model_discovery.py: _TIMEOUT 상수, KeyError 제거, 에러 메시지 내용 노출 방지
- models/model.py + llm_provider.py: list[str] 타입, back_populates 양방향 relationship
- main.py: 시드 모델에 provider_type→provider_id 자동 매핑
- 테스트: InvalidToken fallback + OpenRouter discovery 추가
- 결과: ruff 통과, 329 passed (기존 327 + 신규 2)

## M5 Frontend 리뷰 이슈 수정 완료 (2026-04-06) — Zuckerberg
- lib/utils/provider.ts: getProviderIcon 공통 유틸 추출 (5개 파일 중복 제거)
- models/page.tsx: 삭제 confirm + 개별 isPending (deleteModel.variables === model.id)
- provider-card.tsx: aria-label 2개, confirm, deletingId 개별 비활성화
- provider-form.tsx: try-catch + toast.error, htmlFor/id 4쌍
- model-add-dialog.tsx: try-catch 2개, isError UI, 이미 등록된 모델 표시 (useModels 연동)
- model-select.tsx: role="listbox", role="option" + aria-selected
- agent-node.tsx: 하드코딩 "관리" → i18n ts('manageModels')
- use-providers.ts: deleteProvider → models 캐시 무효화
- use-models.ts: bulkCreate → providers 캐시 무효화
- types/index.ts: ProviderType 유니온 타입 + Provider/ProviderCreateRequest 적용
- ko.json: i18n 키 7개 추가
- 검증: pnpm build PASS + pnpm lint PASS

## Gotchas (M5 Frontend)
- ProviderType 유니온 도입 시 PROVIDER_TYPES(as const)와 Select onValueChange(string) 간 타입 불일치. handleTypeChange를 ProviderType 파라미터로 변경, Select callback에서 as ProviderType 캐스팅.
- provider-card에서 isDeleting 개별화: deletingId prop 추가 + deleteProvider.variables로 전달.

## M6 메타데이터 강화 (2026-04-06) — Jensen

### Task1 Backend 완료
- model_metadata.py: 수동 MODEL_METADATA dict → litellm_model_catalog.json 기반 lazy 로딩
- enrich_model() 시그니처/반환 유지, 새 필드 매핑 추가 (max_output_tokens, supports_vision, supports_function_calling, supports_reasoning)
- ANTHROPIC_MODELS: 하드코딩 3개 → JSON catalog에서 provider=="anthropic" 필터 (18개)
- schemas: DiscoveredModel + ModelResponse에 4개 capability 필드 추가, ModelResponse에 agent_count 추가
- model_service.py: list_models() Agent LEFT JOIN으로 agent_count 집계 + enrich_model() 보강
- model_discovery.py: openai/anthropic/google discover에서 새 필드도 DiscoveredModel에 전달
- 결과: ruff 통과, 329 passed (regression 없음)

### Gotchas (M6)
- ANTHROPIC_MODELS가 JSON 기반이 되면서 3→18개로 확장됨. 기존 테스트에서 `== 3` 하드코딩은 `>= 3`으로 수정 필요.
- enrich_model import를 _model_to_response 함수 내부에서 lazy로 수행 (circular import 방지)

## M6 Frontend UI 개선 완료 (2026-04-06) — Zuckerberg
- types/index.ts: Model에 max_output_tokens, supports_vision/function_calling/reasoning, agent_count 추가. DiscoveredModel에 동일 (agent_count 제외). ModelBulkCreateRequest에도 새 필드 반영.
- model-detail-modal.tsx (신규): 4섹션 Dialog — Overview, Token & Pricing, Capabilities (색상 Badge), Modalities
- models/page.tsx: Capability 필터 Select 추가 (vision/function_calling/reasoning). 모델 카드에 capability 아이콘 (Eye/Wrench/Brain + Tooltip), agent_count 배지, InfoIcon → 상세 모달, 삭제 보호 (agent_count > 0 → disabled + Tooltip). window.confirm → AlertDialog 전환 (모델/Provider 양쪽).
- provider-card.tsx: onDelete 시그니처 () => void로 변경, window.confirm 제거 (부모에서 AlertDialog 처리)
- provider-form.tsx: 테스트 결과 UI 개선 (border + 아이콘 크기 + models_count 표시)
- model-add-dialog.tsx: bulkCreate 시 새 필드 전달 (max_output_tokens, supports_*)
- ko.json: model 네임스페이스에 i18n 키 16개 추가
- 검증: pnpm build PASS + pnpm lint PASS

## 실패 교훈
- S1 삭제 분석에서 streaming.py 필터를 "삭제 가능"으로 판단했으나 S2 심층 조사에서 "유지 필요"로 수정됨. 표면적 분석(미들웨어 구조만 확인)보다 실제 소스코드(PatchToolCallsMiddleware가 before_agent만 구현)를 확인해야 정확한 판단 가능. 삭제 분석 시 방어 코드의 필요성은 실행 경로까지 추적할 것.
