# progress.txt — TTH 팀 공유 메모리

## 프로젝트
Credential/Tools/Skills 그린필드 리라이트 (n8n 패턴 차용, 브랜딩 제거)
- 참조: `PLAN.md` (루트), `/Users/chester/.claude/plans/plan-md-poc-lexical-bumblebee.md`, `CHECKPOINT.md`
- PoC 단계 → dev DB 폐기 OK
- 단일 PR, 마일스톤별 별도 커밋
- 시작: 2026-04-29

## 결정 사항 (불변)
1. Cipher: n8n 알고리즘 차용, HKDF-SHA256(info=`b'moldy-encryption-v1'`), 단일 블롭 Base64(`[version 1B][salt 32B][authTag 16B][ciphertext]`), key_id별도 컬럼
2. LLM 모델: `models` 유지 + api_key_encrypted 제거, `agents.llm_credential_id` 추가, `llm_providers` 폐기
3. 단일 PR
4. Vault provider 실구현 (HVAC SDK, feature flag)

## Codebase Patterns (확인 완료)
- backend: FastAPI + SQLAlchemy async + Alembic. uv. ruff line-length=100 py312
- frontend: Next.js 16 + React 19 + Tailwind v4 + shadcn/ui. pnpm. TanStack Query (서버) + Jotai (클라)
- 테스트: backend pytest + aiosqlite in-memory (DB 불필요). frontend playwright e2e
- 마이그레이션 번호: m12_drop_legacy_columns가 이미 존재 → 신규는 m13_greenfield_credentials
- mock_user 패턴: 인증 없음, get_current_user가 mock 반환
- agent_runtime은 chat_service.build_tools_config()를 통해 인증 해석
- 현 trigger_executor.py L44-46 prefetch 누락 버그 (M5에서 동시 수정)

## n8n 차용 핵심 (NOTICES.md 출처 명기 필수)
- Cipher V2: `packages/core/src/encryption/{cipher,aes-256-gcm}.ts` (Apache-2.0 영역)
- ICredentialType: `packages/workflow/src/interfaces.ts`
- GenericAuth/CredentialsHelper: `packages/cli/src/credentials-helper.ts`
- INodeProperties 동적 폼: `packages/frontend/editor-ui/src/features/ndv/parameters/components/ParameterInput.vue`
- OAuth2 preAuthentication: `packages/cli/src/controllers/oauth/oauth2-credential.controller.ts`
- External Secrets ABC: `packages/cli/src/modules/external-secrets.ee/types.ts`

## 브랜딩 가드 (필수)
- 코드/주석/문서/UI 어디에도 `n8n` 문자열 0건. `\bn8n\b` 정규식이 CI 게이트.
- 패키지 `@n8n/*` import 금지. 동등 generic 라이브러리 (cryptography, httpx, authlib, hvac)
- HKDF info: 절대 `'n8n-encryption-v1'` 쓰지 말고 `'moldy-encryption-v1'`
- frontend 아이콘/로고: Lucide + 자체 SVG. n8n 자산 SHA-256 블랙리스트.
- 화이트리스트: `NOTICES.md` (출처 명기 목적의 단일 파일)

## 파일 경계 (DRI별)
- 피차이: docs/, app/security/cipher.py + key_provider.py, app/models/* (도메인 모델), alembic/versions/m13
- 젠슨: app/{credentials,tools,mcp,skills}/ (도메인 로직), app/routers/, app/services/chat_service.py, app/agent_runtime/, scheduler.py, app/seed/, backend/tests/
- 팀쿡: frontend/src/components/ui/, frontend/src/components/shared/{status-chip,icon,empty-state,dynamic-fields-form}, 디자인 토큰
- 저커버그: frontend/src/app/{credentials,tools,mcp-servers,skills}/, components/{credential,tool,mcp,skill}/, lib/{api,hooks,types}/, frontend/e2e/, sidebar.tsx
- 베조스: scripts/check_branding.py, NOTICES.md, tasks/deletion-analysis.md, 마일스톤 검증, docs/QUALITY_SCORE.md, 회귀 테스트 감독

## 핵심 코드 경로 (재배선 대상)
- chat_service.py:231-259 get_agent_with_tools() — selectinload chain 재작성
- chat_service.py:369-462 build_tools_config() — PREBUILT/CUSTOM 분기 → 단일 경로(definition + credential_id)
- chat_service.py:317-336 _resolve_prebuilt_auth() — 폐기
- chat_service.py:339-366 _resolve_custom_auth() — 폐기 (단일 경로로 흡수)
- trigger_executor.py:44-46 — prefetch 누락 버그 동시 수정
- mcp_client.py — env_vars 템플릿 해석을 app/credentials/interpolation.py로 위임
- model_factory.py — agent.llm_credential 복호화 경로 추가

## 실패 교훈 (누적)

### M3 (Tools + MCP)
- 옛 `services/tool_service.py` (M5에서 삭제)와 `assistant`/`builder` 경로가 `from app.schemas.tool import ToolType, ToolCustomCreate, ToolResponse, ToolUpdate`를 import — 신규 스키마는 별칭 (`ToolInstanceResponse`, `ToolPatch`) 사용하고 schemas/tool.py 하단에 레거시 클래스 + `ToolResponse = LegacyToolResponse` 별칭으로 import-time 안정성 유지. 둘 다 M5에서 폐기.
- 새 Tool ORM은 옛 컬럼 (type/provider_name/connection_id/parameters_schema/api_url/http_method/auth_type/is_system/tags) 모두 제거 — `extend_existing=True`로 메타데이터 재정의 충돌 회피. tests는 `Base.metadata.create_all`로 신규 모델만 빌드.
- main.py lifespan의 `existing_tools_result.where(Tool.is_system.is_(True))` 시드 호출은 신규 컬럼 없음 → 프로덕션 startup이 깨짐. M4(m13 마이그레이션) + M5(seed 재작성)에서 정리. 테스트는 ASGITransport이 lifespan 우회.
- MCP 도메인: stdio transport는 probe 미지원으로 명시(`transport not supported by the probe yet`). 디스커버리는 streamable_http/sse만. langchain-mcp-adapters 미사용 — 직접 mcp SDK + 모듈 단위 monkeypatch로 테스트.
- env_vars/headers 템플릿 해석은 `app.credentials.interpolation.resolve_deep` 단일 진입점 위임 (옛 `agent_runtime/env_var_resolver.py` 미터치, M5에서 삭제).

### M4 (Skills + m18 + 시드)
- 마이그레이션 번호: 기존 체인에 이미 `m13_drop_mcp_legacy.py`가 있어 `m18_greenfield_credentials`로 명명. down_revision=`m17_add_agent_subagents`. PLAN.md 본문은 m13 가정이므로 후속 마일스톤 문서에서 m18로 통일 필요.
- `LLMProvider` 모델은 폐기 대상이지만 `services/{model_service,model_discovery,provider_service}.py`가 임포트 — M5에서 일괄 삭제. 그 전까지는 `back_populates` 관계만 제거하고 metadata 등록은 유지(임포트 호환성).
- `Model` ORM에서 `api_key_encrypted` + `provider_id` 제거. 기존 `default_models.py` dict는 두 키를 안 가지므로 lifespan 시드는 추가 처리 없이 그대로 동작 (방어적으로 lifespan에서 그래도 strip 처리).
- 옛 `tests/test_skill_package.py`는 chat_service의 `build_effective_prompt`/`build_tools_config` 시그니처에 의존 → 신규 Skill 도메인 테스트로 대체 (`test_skills.py` 재작성).
- alembic 마이그레이션 모듈은 `alembic/versions/`에 `__init__.py`가 없어 `importlib.import_module`로 로드 불가 — 테스트는 `importlib.util.spec_from_file_location`로 직접 로드.
- 옛 `services/skill_service.py`는 미터치 (M5 정리). 옛 라우터 의존이 있던 import는 신규 `app/skills/service.py`로 완전 교체.
- 데이터 분실 액션 (alembic upgrade head against postgres)은 사용자 확인 필요 — 본 마일스톤에서는 dry-run/모듈 검증만 수행.

### M7 (모델 카탈로그 + 디스커버리, backend)
- `model_metadata.py`는 옛 코드 그대로 복원 (LiteLLM enrich, anthropic 정적 카탈로그) — 헤더 주석에 LiteLLM 출처 + NOTICES.md 명기.
- `model_filtering.py` 신규 — `should_include_model(provider, model_id, is_custom_api)` + `is_custom_openai_endpoint(base_url)`. OpenAI는 babbage/davinci/computer-use/dall-e/text-embedding/tts/whisper/omni-moderation/sora/-realtime/gpt-*-instruct 블록. 차용 출처 표현은 "prior art — see NOTICES.md"로 통일 (브랜딩 가드 회피).
- `model_discovery.py` 재작성 — `discover_from_credential(db, credential)` 단일 진입점. 옛 LLMProvider 기반에서 Credential 기반으로 재배선. 복호화는 `credential_service.decrypt_with_external` 위임. dispatch table 키 = `credential.definition_key`. OpenRouter pricing 우선(source='openrouter'), LiteLLM fallback(source='litellm'), 무 데이터(source='manual').
- `DiscoveredModel` dataclass에 `already_registered: bool` — IN 쿼리 한 번으로 N+1 회피.
- `models` ORM에 `max_output_tokens, supports_vision, supports_function_calling, supports_reasoning, source` 컬럼 추가. 마이그레이션은 `m19_add_models_source.py` (m18 다음, ADD COLUMN nullable + drop downgrade).
- 라우터 보강: `POST /api/credentials/{id}/discover-models` (audit log `discover` action), `POST/PATCH/DELETE /api/models` (409 unique violation, 409 used by agent).
- Credential 정의 신규: `openrouter` (api_key + base_url default `https://openrouter.ai/api/v1`, test=GET /auth/key), `openai_compatible` (base_url required + api_key optional, test=GET `={{ $credentials.base_url }}/models` with rules [200, 401, 403]).
- 테스트: `test_model_metadata.py`(5), `test_model_filtering.py`(36), `test_model_discovery.py`(9 — httpx.MockTransport via __init__ patch), `test_models_router.py`(13). 합계 63 신규 + 480 회귀 = 543 PASS.
- httpx mock 패턴: `httpx.MockTransport(handler) → patch.object(httpx.AsyncClient, "__init__", ...)` 가 가장 확실. 모듈 단위 patch보다 안정적.
- Decimal pricing → JSON으로 `to_dict()`에서 `str(Decimal)`. Pydantic Decimal 필드가 자동 파싱. FastAPI 응답은 scientific notation 가능 — 테스트는 Decimal로 비교.
- branding 가드: 본문 docstring에 "n8n" 직접 노출 금지. 차용 출처는 "prior art — see NOTICES.md"로 적고 NOTICES.md 표에 매핑 추가.
- frontend는 다른 작업자가 진행 — `frontend/src/lib/api/models.ts`, `frontend/src/components/model/model-select.tsx` 주석에서 'n8n resourceLocator' → 'resourceLocator pattern (see NOTICES.md)'로 정정만 했음.
- `docs/QUALITY_SCORE.md`도 표현 정정 ("n8n 식별자 0건" → "금지 식별자 0건", "n8n 알고리즘 차용" → "차용 알고리즘 (NOTICES.md)").
- 마이그레이션 검증: PG 5433 컨테이너에 upgrade head → 5개 컬럼 추가 확인 → downgrade -1 → upgrade head 라운드트립 모두 PASS.

### M10 (Daily Spend Queue + Aggregate API + Model Fallback, backend)
- `models/{daily_spend_user,daily_spend_agent,daily_spend_model}.py` 신규 — 동일 shape, FK target만 다름. Numeric(20,8) cost (Anthropic Haiku 가격까지 정확). unique (date, target_id), CASCADE on FK → user/agent/model row 삭제 시 aggregate row도 함께 사라짐 (raw token_usages는 유지).
- `m21_add_daily_spend_aggregates` (down=m20) + `m22_add_agent_model_fallback` (down=m21). 둘 다 dialect-aware UUID/timestamp helpers + `_has_table`/`_has_column`/`_has_index` 가드(idempotent upgrade) + reversible downgrade. PG 5433 round-trip(upgrade→downgrade→upgrade) OK.
- `services/spend_writer.py` — `SpendEntry` dataclass + `DailySpendUpdateQueue` (asyncio.Queue + 백그라운드 drain task). 핵심 결정:
  - 단일 큐로 3개 axis 모두 처리 (분리하면 producer가 3번 enqueue 해야 함)
  - flush 트리거: `flush_interval_sec=5.0` 또는 `batch_size=100` 도달 시
  - drain: `wait_for(queue.get(), timeout=interval)` → greedy `get_nowait` until batch_size → `_flush_batch`
  - `_flush_batch`: entries를 axis별 `(date, target_id)`로 group → 각 테이블에 dialect-aware `INSERT ... ON CONFLICT (date, target_id) DO UPDATE SET total_X = X + EXCLUDED.X` (PG/SQLite). 그 외 dialect는 application-level upsert fallback.
  - producer (`add()`) 비차단: `put_nowait` (QueueFull / RuntimeError 시 warning + drop, agent 실행 막지 않음).
  - `stop()` graceful: 큐 잔여 flush → task cancel → await (with `contextlib.suppress`).
  - 모듈 전역 `spend_queue` 싱글턴.
- `hooks/builtin/spend_hook.py` — `agent_invoke` post hook에서 `spend_queue.add(SpendEntry(...))`. 0 token / 0 cost row는 skip (interrupt 등 비정상 종료 가드). failure isolation은 HookRegistry + 자체 try/except 이중 가드.
- `register_default_hooks()`에 SpendHook 추가 → 이제 `["logging_hook", "audit_hook", "spend_hook"]` (`test_hooks.py` 함께 갱신).
- `main.py` lifespan — `spend_queue.start()` (시드+체크포인터 직후, 스케줄러 직전) + `spend_queue.stop()` (스케줄러 shutdown 직후, 체크포인터 shutdown 직전).
- `agent_runtime/streaming.py` — `stream_agent_response`에 `usage_sink: dict | None` 추가. 스트림 종료 시 captured `prompt_tokens/completion_tokens/estimated_cost`를 sink dict에 in-place update.
- `agent_runtime/executor.py`:
  - `AgentConfig.model_fallback_chain: list[dict] | None` 신규 — `[{provider, model_name, base_url, model_id}]` 사전 해석된 형태 (executor는 DB-free).
  - `_build_model_with_fallback(cfg)` — primary → fallback walk + `_is_fallback_recoverable` 분기. 마지막 시도까지 실패하면 마지막 에러 re-raise.
  - `_hook_result_from_usage(duration_ms, usage_sink)` — streaming usage → `HookResult.tokens_in/out/cost_usd` 매핑 (None-safe).
  - `execute_agent_stream/resume_agent_stream`이 `usage_sink={}`를 stream에 전달 → 종료 후 hook post에 매핑된 HookResult 전달 → SpendHook이 큐에 enqueue.
- `agent_runtime/model_factory.py`:
  - `create_chat_model_with_fallback(agent, db, *, api_key, base_url, **extra)` 신규 — primary attempt → recoverable error 시 `agent.model_fallback_list` walk + audit log. ORM-backed 경로 (executor와 별개). 실제로 executor는 `_build_model_with_fallback`를 사용하고, 이 ORM 헬퍼는 외부 호출자가 직접 사용 가능 (assistant/builder 등 후속).
  - `_is_fallback_recoverable(exc)` — `status_code` 우선 (401/403/404/408/409/429/500/502/503/504), 타입 fallback (TimeoutError/HTTPError/HTTPStatusError/ConnectionError). string heuristic은 의도적으로 제외 (불안정).
  - `_audit_fallback_attempt` — `credential_audit_log`에 `action='fallback'` row 기록 (`success: bool`, `provider`, `model_name`, `model_id`, `error`, `phase: 'attempt'`). agent.llm_credential_id 없으면 skip. audit 실패는 warning + 진행 (runtime 절대 안 막음).
- `routers/conversations.py` + `agent_runtime/trigger_executor.py` — `_resolve_fallback_chain(db, fallback_list)` 헬퍼: `agent.model_fallback_list` (UUID strings) → `Model` row 조회 → `[{provider, model_name, base_url, model_id}]` 사전 해석. 누락된 row는 silent drop. `AgentConfig.model_fallback_chain`에 주입.
- `models/agent.py` — `model_fallback_list: Mapped[list[str] | None]` (JSON, nullable). UUID 직렬화는 service 레이어에서 (Postgres 호환).
- `schemas/agent.py` — `AgentCreate/Update/Response`에 `model_fallback_ids: list[uuid.UUID] | None` 추가. Response는 default factory `[]` (None은 안 보냄).
- `services/agent_service.py` — `_validate_model_fallback_ids()` (existence check 한 번 — catalog는 user-shared). Create/Update에서 검증 + str(UUID) 직렬화.
- `routers/agents.py` — `_agent_to_response()`에서 `model_fallback_list` (str list) → `model_fallback_ids` (UUID list) 변환. 잘못된 entry는 silent skip.
- `services/usage_aggregate.py` 신규 — `get_daily_spend()` single entry-point. Tenancy:
  - user 축: `DailySpendUser.user_id == current_user`
  - agent 축: `DailySpendAgent → Agent` join + `Agent.user_id == current_user`
  - model 축: per-model 테이블은 cross-user이므로 (per-agent 테이블) → Agent join → user 필터로 *현 사용자 contribution*만 집계. `group_by=target` 시 `Agent.model_id`로 추가 group_by.
  - `_label_map(target_kind, ids)` — user/agent/model 각각의 라벨 (name / display_name / email).
  - `_resolve_window(from, to)` — default = (today-30d, today), max lookback 365d clamp.
- `routers/usage.py` 보강 — `GET /api/usage/daily?target_kind=user|agent|model&target_id?&from?&to?&group_by=date|target`. mock_user 격리.
- 테스트:
  - `test_spend_writer.py`(8 — flush_batch/ON CONFLICT 누적/axis skip/loop interval/stop drain/queue full/distinct dates/Decimal precision)
  - `test_usage_aggregate.py`(6 — user 시계열/agent label/model agent-scope/window/target_id/cross-tenant isolation)
  - `test_model_fallback.py`(9 — classifier 3종/executor chain 4종/with_fallback audit 2종)
  - `test_migration_m21.py`(4) + `test_migration_m22.py`(4) — module imports/metadata/round-trip/idempotent
- 합계 31 신규 + 603 회귀 = 634 PASS, ruff clean, branding 0건. PG 5433에서 m20 → m21 → m22 → downgrade -2 → upgrade head 라운드트립 성공.
- 발견 이슈 / 학습:
  - `from app.agent_runtime.model_factory import create_chat_model`는 executor 모듈의 로컬 namespace에 reference 복사 → `patch.object(model_factory, ...)`로는 안 잡힘. test에서 `patch("app.agent_runtime.executor.create_chat_model", ...)`로 사용 위치를 패치해야 함.
  - `app.credentials.cipher`는 존재하지 않음 (실제 위치는 `app.security.cipher`). credential 암호화는 `app.credentials.service.encrypt_data()`를 통해 호출.
  - SQLite의 ON CONFLICT는 3.24+에서 지원되며 `from sqlalchemy.dialects.sqlite import insert`의 `excluded.<col>` 표현식이 PG와 동일하게 동작. application-level fallback은 다른 dialect에 대비한 안전망.
  - executor의 fallback walk를 ORM 의존 없이 만들기 위해 `AgentConfig.model_fallback_chain`을 caller(라우터/트리거)가 사전 해석. ORM-backed `create_chat_model_with_fallback`은 별도 외부 호출자용으로 보존 (audit log 포함).

### M10 (Spend Dashboard + Model Fallback UI, frontend)
- `lib/types/usage.ts` 신규: `UsageDailyEntry|UsageDailyParams|UsageTargetKind|UsageGroupBy|UsageMetric`. 백엔드 `/api/usage/daily` shape을 그대로 미러. `index.ts`에서 `export *`.
- `Agent` 타입에 `model_fallback_ids?: string[] | null` 추가. `AgentCreate/UpdateRequest`에도 동일. 백엔드 컬럼명은 `agents.model_fallback_list` (Postgres ARRAY UUID), 프론트는 `model_fallback_ids` 명명을 사용 → backend sub-agent가 schema 변환 필요(우리는 frontend payload를 명세대로 보냄).
- `useDailyAggregate(params)` cache key는 params 객체 그대로 사용 → TanStack Query가 deep-equal로 invalidation 인식.
- 차트는 `M9 health-history-chart` 패턴 그대로 차용 — `viewBox` 기반 SVG, 자체 path/rect/area, native `<title>` 툴팁, accent 색은 emerald-500/blue-500/purple-500. 의존성 추가 0건 (Recharts/Tremor/Chart.js 모두 미사용).
- cost 표시: `formatCostUsd` 매그니튜드 적응형 — `>=1` → 2 decimals, `>=0.01` → 4, 그 외 → 6. 토큰은 `>=1M` `M`, `>=10K` `K` 줄임.
- `details/summary` 네이티브 disclosure로 fallback 섹션 접기/펴기. shadcn에 popover/switch/collapsible이 없는 상태라 native HTML 요소 활용으로 추가 의존 회피.
- 순서 변경은 위/아래 화살표 버튼 (dnd-kit 미도입). 5개 cap. 중복 fallback (Primary와 또는 다른 fallback과 같음)은 amber 경고만 표시 — 저장은 허용 (백엔드가 필터링).
- ModelDialog의 `t={tu}` prop typing은 `useTranslations<'usage.fallback'>` 반환 타입으로 typing — 일반 함수 시그니처는 next-intl의 `Translator` 제네릭과 호환 안 됨 (variants/values param이 unknown vs 정확한 union).
- settings page의 dirty-aware sync 패턴: agent 새로 도착 시 `setFallbackIds(agent.model_fallback_ids ?? [])`. 같은 agent refetch 시 `arraysEqual(fallbackIds, prev.model_fallback_ids ?? [])` true이면만 새 server값 반영. PATCH body에는 `fallbackIds.length > 0 ? fallbackIds : null`로 변환 (백엔드 nullable column).
- `playwright.config.ts`에 `PW_SKIP_BACKEND=1` env로 backend webServer 스킵 토글 추가. 모든 spec이 `page.route` mock이라면 backend 불필요. 새 spec(spend-dashboard, model-fallback) + M9 health-check도 fully mocked.
- E2E 패턴: backend route 모두 mock → Playwright `page.route('**/api/...')`. UPDATE 시 PATCH body 캡처는 `route.request().postDataJSON()`. settings 페이지는 `useAgent + useModels + useTools + useSkills + useMiddlewares + useTriggers` 6 endpoints 모두 mock 필요.
- locator 함정: `page.getByDisplayValue` 미지원 → `page.locator('input[placeholder]').first()` + `toHaveValue()`. `page.getByText('label')` strict mode 위반 (차트 + 테이블 양쪽 렌더) → `barChart.getByText('label')`로 scope.
- 발견 이슈: 로컬 PG가 m17에 머물러 있어 backend lifespan에서 `column models.max_output_tokens does not exist` 발생 (m19에서 추가된 컬럼). e2e는 `PW_SKIP_BACKEND=1`로 우회. backend sub-agent가 head를 m22까지 가져갔으니 `alembic upgrade head` 적용 시 정상화.

### M9 (Hook/Middleware 프레임워크 + Health Check + History, backend)
- `app/hooks/{base,registry}.py` 신규 — `CustomHook` 베이스 (concrete, 빈 메서드를 옵셔널 노옵으로 노출 → ABC 패턴 회피로 ruff B024/B027 우회), `HookRegistry`는 모듈 전역 싱글턴 `hooks` + `register/all/clear/run_pre/run_post/run_failure`. 등록은 `name` 기준 idempotent (중복 등록 시 마지막 인스턴스로 교체). 디스패치는 등록 순서대로 실행하며 hook 예외는 `logger.warning` + `continue` (failure isolation).
- 빌트인 hook: `LoggingHook`(INFO 라인) + `AuditHook`(`credential_audit_log` `action='invoke'` row 생성, `source='runtime'`, `log_metadata.phase` ∈ {post, failure}). AuditHook은 `ctx.credential_id`가 있을 때만 row 생성 — pre는 row 미생성으로 audit 트래픽 절감.
- `register_default_hooks()` → `app/main.py` lifespan에서 호출. 시드/스케줄러 시작 직전.
- `agent_runtime/executor.py`: `AgentConfig`에 `user_id`/`model_id`/`llm_credential_id` 추가. `_hook_ctx_for_agent`가 cfg에 user_id가 있을 때만 ctx 생성. `execute_agent_stream`/`resume_agent_stream`/`execute_agent_invoke` 셋 다 try/except로 wrap → 성공 시 `run_post(duration_ms)`, 실패 시 `run_failure` + re-raise.
- `agent_runtime/tool_factory.py`: `tools_config` 항목당 user_id/agent_id/tool_id/credential_id를 `_safe_uuid`로 보강한 ctx 생성. `_invoke` 내부에서 pre/post/failure dispatch. `output`은 200자 cap.
- `mcp/client.py`: `connect_and_list`에 `user_id`/`mcp_server_id`/`credential_id` keyword 추가. 모든 early-return 경로(transport unsupported / url missing / SDK import 실패) + 성공/실패 모두 hook dispatch. 기존 호출자(`test_chat_integration`)는 user_id 없이 호출 → ctx None → no-op (회귀 X).
- `services/chat_service.py`: `build_tools_config`가 `user_id`/`agent_id`를 tools_config 항목에 추가. `routers/conversations.py` + `agent_runtime/trigger_executor.py`가 AgentConfig에 user_id/model_id/llm_credential_id 채움.
- `models/health_check_history.py` 신규 — `id, target_kind ('model'|'mcp_server'), target_id, status ('healthy'|'unhealthy'|'degraded'), latency_ms, error_kind, error_message, raw_result, checked_at`. 인덱스 `(target_kind, target_id, checked_at)`. FK 안 검 (단순화).
- `services/health_check.py` 신규 — `check_model`(M8 `model_test.run_model_test` 재사용 → 결과 분류 → history row), `check_mcp_server`(`mcp.client.connect_and_list` 호출 → `_classify_mcp_error`로 auth/timeout/etc 분류 → history + McpServer 상태 업데이트), `check_all_active`(모든 model + active MCP 순회). degraded는 MCP `auth_needed` 전용.
- `routers/health.py` 신규 — `/api/health/models`, `/api/health/mcp-servers`(latest only, in-process reduce로 N+1 회피), `/api/health/history?target_kind&target_id&limit≤200`, `POST /api/health/check`(즉시 실행 + history 반환). MCP는 user 격리, model은 글로벌(공유 카탈로그).
- `scheduler.py`: `register_health_check_job()` + `health_check_all_active()` 비동기 헬퍼. `HEALTH_CHECK_JOB_ID = "health_check_sweep"`, `coalesce=True, max_instances=1`, default cron `"0 4 * * *"`.
- `config.py`: `health_check_cron`, `health_check_history_retention_days=90`(정리 cron은 후속).
- `alembic/versions/m20_add_health_check_history.py`: dialect-aware UUID/timestamp helper + `_has_table`/`_has_index` 가드(idempotent upgrade). Reversible downgrade(테이블+인덱스 drop). PG 5433 라운드트립(upgrade→downgrade→upgrade) OK 확인.
- 테스트: `test_hooks.py`(8 — registry 순서/idempotent register/failure isolation/disabled hook/LoggingHook smoke/AuditHook credential gate + failure recording), `test_health_check.py`(10 — model success/failure 분기, MCP success/auth/unreachable + 서버 상태 업데이트, sweep counters, 라우터 history/summary/check-now/404), `test_migration_m20.py`(4 — module imports/metadata/round-trip/idempotent upgrade). 합계 22 신규 + 581 회귀 = 603 PASS.
- AuditHook은 `app.hooks.builtin.audit_hook.async_session`을 호출하므로 테스트에서 `monkeypatch.setattr(...)`로 `TestSession` 주입 필요. mock_user 패턴 답습.
- ruff B024/B027(ABC 빈 메서드) 우회: `CustomHook`을 `ABC` 상속 안 함 — concrete base + 빈 default = 노옵. 의미적으로는 abstract 의도이나 ruff는 ABC 상속 시 명시적 `@abstractmethod` 강제.
- NOTICES.md에 LiteLLM 차용 영역 추가 (CustomLogger 인터페이스 패턴 + `/health/history` 응답 형태).

### M8 (모델 Test + Curl 생성 + MCP Registry, backend)
- `app/agent_runtime/model_factory.py`에 `create_chat_model_for_test(provider, model_name, *, api_key, base_url)` 추가 — `max_tokens=10`, `temperature=0` 강제 (결정론·저비용). `_ssl_ctx`는 기존 경로 재사용.
- `app/services/model_test.py` 신규 — `run_model_test()` + `ModelTestResult`/`ModelTestError`. `asyncio.wait_for(timeout=30)` 강제, prompt = `"Reply with just the word 'pong'."`. clean error 정규식: `litellm|openai|anthropic|google|httpx`prefix 제거 + `Error code: NNN -` 제거 + 300자 cap. classify는 status_code(401/403/404/429) 우선, message hint fallback. raw_request는 SDK 미노출이라 provider별로 reconstruct (Authorization → `Bearer ***`, x-api-key → `***`, Google `?key=***`). curl은 `${API_KEY}` placeholder로 다시 치환 — 어떤 surface에도 평문 키 미노출.
- 응답 스키마(`app/schemas/model.py`): `ModelTestPreviewRequest`, `ModelTestErrorSchema`, `ModelTestResponse`. `model_config = ConfigDict(protected_namespaces=())`로 `model_*` 필드 경고 회피.
- 라우터(`app/routers/models.py`): `POST /api/models/{model_id}/test?credential_id=...` (등록된 모델 + cost lookup), `POST /api/models/test-preview` (Custom ID flow, body로 provider/model/credential_id 받음). 둘 다 `credential_audit_log` `action='test'`에 metadata.success/model_id/preview 기록.
- `app/data/mcp_server_registry.json` 신규 (5개 entry: github, linear, jira, slack, notion). 순수 JSON, 주석 미포함. stdio entry는 `${credential.<field>}` env_var 템플릿 사용.
- `app/services/mcp_registry.py` 신규 — `_load_registry()` lazy + cached, `list_registry()` 알파벳 정렬, `get_registry_entry(key)`, `reset_cache()` (테스트용).
- `app/schemas/mcp.py`: `McpRegistryEntry`, `McpServerCreateFromRegistry` 추가.
- `app/routers/mcp.py`: `catalog_router = APIRouter(prefix="/api/mcp-server-types")` + `GET /` `GET /{key}` 2건. `POST /api/mcp-servers/from-registry`는 기존 router에 추가 — registry entry로 transport/url/command/args/env_vars pre-fill. unknown key는 400.
- `app/main.py`: `app.include_router(mcp.catalog_router)` 추가 (mcp.router와 별도 mount).
- `NOTICES.md`: LiteLLM 섹션에 `model_test.py` 추가 (probe + clean error + curl 패턴), 신규 "MCP Server Catalog" 섹션 추가 (자체 작성).
- 테스트: `test_model_test.py`(27건 — clean error/classify/curl/timeout/auth/not_found/rate_limit/router 성공·404·credential required·preview·audit log·leak detector), `test_mcp_registry.py`(11건 — service 4 + catalog router 3 + from-registry 4). 합계 38 신규 + 543 회귀 = 581 PASS, ruff clean, branding 0건.
- mock 패턴: LangChain 레이어에서 stub. `patch.object(ChatOpenAI, "ainvoke", _stub)`. usage_metadata 인스턴스 속성으로 set (`AIMessage.usage_metadata = {...}`). PROVIDER_MAP 모두 ChatOpenAI fallback이라 한 곳만 패치하면 충분.
- 보안 가드: `_REDACTED = "***"`, `_CURL_PLACEHOLDER = "${API_KEY}"`. raw_request 헤더에는 마스킹된 값이 들어가고 curl은 placeholder로 한 번 더 치환. test에서 `sk-LEAK-DETECTOR` 키로 응답 전체를 스캔해 평문 누출 검증.

### M5 (agent_runtime 재배선 + 키 로테이션)
- chat_service.build_tools_config 시그니처가 async로 변경됨 — caller(`conversations.py`, `trigger_executor.py`)에서 `await` 필요. 기존 sync caller 검색 시 주의.
- `Tool` ORM에 `credential` 관계 추가 (lazy="select"), `Agent`에 `llm_credential` 관계 추가. selectinload 체인은 두 관계를 명시적으로 로드.
- agent_runtime/mcp_client.py는 thin compat wrapper로 변환됨 — 진짜 로직은 `app.mcp.client.connect_and_list`. 신규 코드는 후자를 직접 import.
- model_factory.PROVIDER_API_KEY_MAP은 backwards-compat alias로만 유지 (Builder/Assistant sub-agent helpers가 사용). 새 chat runtime은 Agent.llm_credential 복호화 경로 사용.
- 키 로테이션 cron: APScheduler 잡 ID `credential_rotation`, default `0 3 * * 0`. `rotate_credentials_to_active_key`는 100건 배치 페이지네이션, 실패 row는 log 후 다음 회차 재시도. `re_encrypt_with_active_key` 헬퍼는 `app.credentials.service`에 거주.
- `tool_service.py` / `model_service.py`는 read-only 슬림화 (Builder/Assistant catalog 한정). CRUD/Provider/Discovery 경로는 모두 폐기.
- 옛 `m13` 마이그레이션 번호는 M4가 `m18`로 받음. M5는 마이그레이션 미터치.
- 회귀 시나리오: tests/test_chat_integration.py 3종(get_agent_with_tools 단일 경로 / trigger prefetch 일치 / MCP 인터폴레이션) + tests/test_rotation.py 2종.
- 옛 `provider_id` / `api_key_encrypted` Model 컬럼은 M4 ORM에서 이미 사라졌고 `default_models.py`는 lifespan에서 strip 처리. tests/`test_models.py`/`test_providers.py`/`test_model_*` 등은 모두 삭제.

### M2 (Credential 도메인)
- TestRequestSpec 이름이 pytest collection 경고 발생 — 후속 cleanup으로 CredentialTestSpec 등으로 rename 권장
- aiosqlite는 FK CASCADE 자동 enforce 안 함 — PRAGMA 또는 PostgreSQL 환경에서만 검증. 테스트는 credential row 삭제만 assert
- OAuth2 callback state는 in-process map (PoC 한정). 멀티프로세스 배포 시 Redis/DB 백킹 필요
- 옛 `services/credential_service.py`, `connection_service.py`는 미터치 — M5에서 일괄 삭제 (기존 ~46 tests fail은 의도된 상태)
- `app/main.py` lifespan에 레거시 `seed_mock_user_prebuilt_connections` 호출 잔존 — M5에서 정리

## 후속 cleanup 항목 (PR 머지 전)
- TestRequestSpec → CredentialTestSpec rename
- OAuth2 state Redis/DB 백킹 (별도 티켓)
- aiosqlite FK PRAGMA conftest 추가 검토

## 작업 순서
M0(거버넌스+docs+삭제분석) → M1(브랜딩+Cipher) → M2(Credential+Vault+라우터) → M3(Tools+MCP) → M4(Skills+m13) → M5(agent_runtime 재배선+cron) → M6(프론트엔드)

각 마일스톤 PASS 후 다음 진입. 단일 PR이지만 마일스톤별 커밋.

## ── 2026-05-01 새 프로젝트: UI Refactor (Sprint 1~4) ──

### 컨텍스트
- Plan: ~/.claude/plans/buzzing-prancing-cloud.md
- Scope: frontend/만. 백엔드/DB 변경 없음.
- 핵심 동기:
  1) 다이얼로그 크기·디자인 일관성 부재 (27곳 산재, 8곳은 콘텐츠에 따라 늘어남)
  2) Sheet 4종(credential/skill/tool/mcp) → Dialog로 전면 전환 요구
  3) 강조색이 emerald raw로 58곳 박힘 → `--primary`로 토큰화. 채팅 사용자 메시지(`bg-emerald-100/dark:bg-emerald-900`)가 새 primary가 됨
  4) 포커스 링 진한 테두리 거슬림 → `border-ring` 제거 + ring-3→ring-2 + ring/50 알파 토큰화
  5) 다이얼로그 비주얼 정비 (헤더 아이콘 슬롯, 푸터 subtle bg, rounded-2xl, X 버튼 통일)
  6) 부수: PageShell, BaseDetailDialog, agents settings RHF+Zod, bundle splitting, 임의값 350+개 정리

### 사일로 구성 (사티아 결정)
- 팀쿡: DialogShell 비주얼 스펙 + 디자인 토큰 oklch 픽스 (Sprint 1-1, 1-3, 1-4 디자인 부분)
- 저커버그: 모든 코드 구현 (Sprint 1~4)
- 베조스: Sheet 사용처 검증 + 삭제 분석 + 마일스톤 게이트 검증

### 결정 사항 (불변)
- Sheet은 모바일 사이드바(`ui/sidebar.tsx:178`)와 대화목록(`conversations/[conversationId]/page.tsx:118`) 2곳만 유지. 나머지 4 detail은 Dialog로 변환.
- 새 `--primary`는 채팅 사용자 메시지 박스 색을 그대로 사용 (라이트 emerald-100 / 다크 emerald-900). `--primary-strong`는 텍스트/링크/탭 인디케이터용 (emerald-600/400).
- 포커스 링: `focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50` → `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-0` + `--ring` 자체에 알파.
- DialogShell이 모든 다이얼로그의 비주얼 일관성 강제. 27개 DialogContent 사용처는 모두 마이그레이션 대상.
- DIALOG_SIZE: sm(400) / md(560) / lg(720) / xl(920) / console(1080). DIALOG_HEIGHT: auto(480) / fixed(640) / tall(760). 콘텐츠 무관 고정.

### S1 베조스 발견 (2026-05-01)
- Sheet 사용처: 6곳 (4 detail + sidebar mobile + conversation list) + ui/sheet.tsx 정의. 추가 없음.
- 4 detail-sheet prop 시그니처 동일 패턴 `{<entity>Id, open, onOpenChange}`. mcp만 `serverId`로 도메인 명명 유지.
- ⚠ **[정정]** `components/shared/page-header.tsx` 9개 페이지에서 사용 중(settings, settings/system-credentials, tools, agents/new/template, usage, models, skills, mcp-servers, credentials). 절대 삭제 금지. (이전 분석의 grep "PageHeader" 결과 오류였음 — 실사용 9건)
- ⚠ skill-detail-sheet 이중 SheetContent 구조: 외부 wrapper(L42) + 내부 SkillDetailBody(L108) `key={skillId}` 리셋 패턴. Dialog 변환 시 단일 BaseDetailDialog로 통합 + key 패턴은 컴포넌트 내부에서 maintain.
- 4개 detail의 삭제 인라인 UI 클래스 100% 동일 → DeleteConfirmInline 공용화 안전.
- 보고서: tasks/sheet-deletion-analysis.md

### S2 팀쿡 결정 (2026-05-01) — ADR-010
- oklch 정확값 (Tailwind v4 emerald palette 그대로):
  - 라이트: --primary=oklch(0.95 0.052 163.051), --primary-foreground=oklch(0.262 0.051 172.552), --primary-strong=oklch(0.596 0.145 163.225), --ring=oklch(0.596 0.145 163.225 / 0.4)
  - 다크: --primary=oklch(0.378 0.077 168.94), --primary-foreground=oklch(0.95 0.052 163.051), --primary-strong=oklch(0.765 0.177 163.223), --ring=oklch(0.765 0.177 163.223 / 0.45)
- 상태색: success=primary-strong, info=sky-500/400, warn=amber-500/400, danger=red-500/400, accent=violet-500/400
- 포커스 링: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring` (border-ring 제거, ring/50 알파 토큰 내장)
- 트레이드오프: 사용자 메시지를 `--user-bubble` 별도로 분리하지 않고 `--primary`로 승격. `bg-primary text-white` 가정 코드 발견 시 `text-primary-foreground`로 교정.
- 상세 스펙: docs/design-docs/ADR-010-ui-tokens-and-dialog-shell.md

### S3 저커버그(직접 구현) 완료 (2026-05-01)
- globals.css에 emerald primary + primary-strong + status-* 토큰 추가. @theme inline 매핑 완료. pnpm build 1차 PASS.
- focus 링 완화 5개 파일 (input/textarea/select/button/checkbox): `focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50` → `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`. border 변경 X.
- ui/dialog: rounded-xl→rounded-2xl, ring-foreground/10→ring-border/60, shadow-2xl 추가, backdrop bg-black/40 + backdrop-blur-sm. DialogFooter도 muted/50→muted/30.
- ui/sheet: 백드롭만 통일 (bg-black/40 + backdrop-blur-sm). 변환 후 사이드 모서리는 사용처에서 적용.
- shared/ 신규 6개: error-state, dialog-shell, delete-confirm-inline, form-footer, page-shell, base-detail-dialog. 모두 build PASS.
- DialogShell은 Object.assign으로 namespace 패턴 (Header/Body/Footer/Sidebar/Split). 사용 시 `<DialogShell.Header />` 식.
- BaseDetailDialog: id 변경 시 useEffect로 confirming false 자동 리셋 (베조스 ⚠ skill-detail key={skillId} 패턴 흡수).
- 핵심 발견: shadcn이 아니라 base-ui (`@base-ui/react/dialog`). API 호환되지만 클래스 패턴이 `data-open:` `data-closed:` 식.
- Button에 이미 `emerald`/`emeraldStrong` variant 존재 → Sprint 2에서 `default`로 흡수 후 제거.

### S4 저커버그(직접 구현) 완료 (2026-05-01)
- 4개 detail-dialog 신설 (credential/skill/tool/mcp-server-detail-dialog.tsx). 각각 DialogShell 직접 사용. 비즈니스 로직 많아 BaseDetailDialog 슬롯에 끼우는 것보다 직접 작성이 더 깔끔.
- 호출부 4 페이지 import + 컴포넌트명 교체. 기존 *-detail-sheet.tsx 4 파일 삭제.
- ⚠ react-19 ESLint 룰 `react-hooks/set-state-in-effect`: useEffect에서 setState 직접 호출 금지. 해결: Inner 컴포넌트 분리 + 상위에서 `key={id ?? 'closed'}` remount 패턴. 4개 detail-dialog + base-detail-dialog 모두 적용.
- SheetContent 잔존 검증: ui/sidebar.tsx + conversations/[conversationId]/page.tsx 두 곳만. 베조스 명세와 일치.
- pnpm lint clean + pnpm build PASS.
- 다이얼로그 size: credential/tool/mcp=lg(720), skill=xl(920). 높이는 모두 fixed(640) 또는 mcp만 tall(760).

### M-UI1 부분 done (2026-05-01)
- 핵심 가치(사용자 요구) 달성: 시트→다이얼로그 전환 + emerald primary 토큰 + 포커스 링 완화 + 다이얼로그 비주얼 일관성(rounded-2xl, ring-border/60, shadow-2xl, backdrop-blur, X 버튼).
- 잔여 작업: 27개 DialogContent → DialogShell 마이그레이션 (M-UI1b로 분리). 점진적 가능. 새 다이얼로그는 DialogShell만 쓰면 자동 일관성.
- Plans are Disposable: 한 번에 27개 변환은 컨텍스트/회귀 위험. 핵심 인프라 + 4개 detail까지가 1세션 한계.

## ── 2026-05-01 후반 — MCP/Skill 고도화 (M-MCP1/M-SKILL1 시작) ──

### Plan
- 위치: ~/.claude/plans/buzzing-prancing-cloud.md (덮어쓴 새 plan)
- 동기: LambChat(`/Users/chester/dev/ref/LambChat`) 코드 분석 후 우리 MCP/Skill의 5가지 격차 식별:
  1) stdio transport 미지원
  2) Import/Export 부재
  3) health polling 부재
  4) Skill 파일 단위 API 부재 (content 통째로만)
  5) frontmatter 자동 동기화 부재

### S16 백엔드 기초 (이번 세션 완료)
- **alembic m26_mcp_health_and_system.py**:
  - mcp_servers: is_system(bool, default false) + health_status + health_polled_at + health_message
  - mcp_tools: last_seen_at (stale 도구 마킹용)
- **app/skills/service.py**: set_skill_file / delete_skill_file 추가 (package skill 한정), inspector._resolve_safely 재사용으로 zip-slip 동일 방어, _refresh_package_metadata로 size/files 자동 갱신, _sync_frontmatter로 SKILL.md 변경 시 description/version/name 자동 동기화
- **app/schemas/skill.py**: SkillFileUpdate
- **app/routers/skills.py**: PUT /api/skills/{id}/files/{path} (텍스트 갱신/생성), DELETE /api/skills/{id}/files/{path} (SKILL.md 보호), POST /api/skills/{id}/files (multipart 업로드)
- 검증: pytest 674 passed (회귀 0), ruff clean, alembic upgrade head 성공
- 발견: inspector.py에 이미 `parse_skill_md(frontmatter 라이브러리 사용)` 있어서 별도 parser.py 신설 불필요. `_resolve_safely` private 함수를 service에서 재사용

### 다음 세션 우선순위 (남은 4개)
- #17 (백엔드): stdio transport (mcp/client.py:86-92 unsupported 분기 제거), discovery stale 마킹, mcp import/export 엔드포인트(Claude Desktop 호환), scheduler.register_mcp_health_job
- #18 (프론트): skill-detail-dialog DialogShell.Sidebar + 멀티파일 에디터 (좌 SkillPackageTree, 우 에디터, dirty 마킹)
- #19 (프론트): mcp-server-wizard 3탭(Basics/Auth/Tools) + transport별 조건부 + 도구 stale 배지 + Import 다이얼로그
- #20 (검증): backend pytest + frontend lint/build + 시각 회귀

## ── 2026-05-01 후반(2) — MCP/Skill 잔여 작업 (/tth 풀스택) ──

### 사일로 구성 (사티아 결정)
- 젠슨(백엔드 DRI): #17 stdio + discovery stale + import/export + health polling + agent_runtime enabled 필터 + skill prompt 주입
- 저커버그(프론트 DRI): #18 skill 멀티파일 에디터 + #19 mcp wizard 재설계 + mcp import dialog
- 베조스(QA DRI): #20 최종 검증 (pytest + lint + build + 시각 회귀)

### 결정 사항 (불변)
- 브랜치: feature/dialog-shell-and-mcp-skill-base 위에 추가 커밋
- DialogShell + 디자인 토큰 인프라(Sprint 1 완료)를 본격 사용
- 백엔드 m26 마이그레이션 + skill file-level CRUD는 ed6ba50에서 이미 완료 → 이번엔 위에 빌드
- import/export JSON은 Claude Desktop `{mcpServers: {...}}` 호환 + 우리 확장(transport, credential_id) 허용
- stdio transport는 mcp.client.stdio.stdio_client 사용 (mcp SDK)
- mcp-upload-dialog는 skill-create-dialog의 Package 탭으로 흡수 후 삭제

### 파일 경계
- 젠슨: backend/app/{models,schemas,routers,mcp,services,scheduler,agent_runtime,skills}/* 만 수정
- 저커버그: frontend/src/* 만 수정 (백엔드 호출만, ts 타입 추가는 OK)
- 베조스: 검증만, 코드 수정 X (발견 이슈는 ? 프로토콜)

### S18 저커버그(직접 구현) 완료 (2026-05-01) — Skill 멀티파일 에디터 + Create 3탭
- skill-detail-dialog: DialogShell.Split + Sidebar(260px) + Body로 패키지 skill 멀티파일 편집. text skill은 단일 Textarea 유지(분기). dirty Map<path,content> + remoteCache Map으로 파일별 저장 상태 관리, 다른 파일 클릭 시 unsaved 있으면 confirm. 텍스트/이미지/PDF/binary 분기, SKILL.md는 protected(삭제 hide).
- skill-create-dialog: DialogShell size=lg height=fixed + 3탭 Tabs(line variant). Text/Package/From-scratch. Scratch 탭은 jszip으로 SKILL.md만 든 zip을 in-browser 빌드 후 /api/skills/upload → onCreated(id) 콜백으로 detail-dialog 자동 오픈.
- skill-upload-dialog.tsx 삭제, skills/page.tsx의 호출부 단일화(`openCreate('text')` + onCreated→setDetailId).
- React 19 useEffect setState 회피: loadingFile은 derived(`!cache.has(path)`), 비동기 fetch then/catch 내 setState만 사용.
- 의존성: jszip 3.10.1 + @types/jszip 추가. cn() 함정 회피용 명시 sm:max-w-none 불필요(DialogShell이 흡수).
- 검증: pnpm lint clean, pnpm build PASS (16 라우트).

### S17 젠슨(백엔드) 완료 (2026-05-01)
- **stdio transport (S17a)**: app/mcp/client.py에 _connect_stdio 헬퍼 추가 (asyncio.wait_for + settings.mcp_connection_timeout). discovery._probe가 server.command/args/env_vars도 같이 forward.
- **stale 도구 보존 (S17b)**: discovery.discover_tools — 사라진 도구 row는 삭제하지 않고 last_seen_at만 stale (agent_mcp_tools 링크 보호). McpToolResponse에 last_seen_at, McpServerResponse에 is_system/health_status/health_polled_at/health_message 노출.
- **import/export (S17c)**: POST /api/mcp-servers/import (Claude Desktop {mcpServers:{...}} 호환, command만 있으면 stdio 추론, overwrite=false면 skip 카운트). GET /api/mcp-servers/export (credential_id만 노출, 비밀값 미포함, 헤더 템플릿 보존). 라우트는 /{server_id} 위에 등록 (UUID 매칭 충돌 회피).
- **health polling job (S17d)**: scheduler.poll_mcp_servers_health + register_mcp_health_job (interval 5분, settings.mcp_health_check_interval_minutes). main.lifespan에 등록 추가. discovery.test_server를 host 함수로 호출 → health_status/health_polled_at/health_message 갱신.
- **runtime + skill prompt (S17e)**: chat_service의 MCP enabled=False 필터는 이미 존재 (line 330-331). app/skills/prompt.py 신설(build_skills_prompt, dict/ORM 모두 수용). executor가 cfg.agent_skills 있을 때 "## Available Skills" 블록을 system_prompt에 append. service.to_runtime_dict에 description 추가.
- 핵심 발견: import 라우트는 반드시 /{server_id} 동적 라우트 **위에** 두어야 함 (FastAPI는 import/export 문자열을 UUID로 파싱 시도 → 422). 기존 test_discover_drops_stale_tools는 새 동작에 맞게 preserves_stale_tools_and_marks_last_seen으로 재작성 + test_chat_integration의 stdio=unsupported 가정 테스트도 websocket으로 변경.
- 검증: ruff clean + pytest 684 passed (base 674 + 신규 10개, 회귀 0).

### S19 저커버그(직접 구현) 완료 (2026-05-01) — MCP wizard 재설계 + Import dialog + tools 확장
- **타입/api/hooks**: McpServer에 is_system/health_status/health_polled_at/health_message 추가, McpTool에 last_seen_at 추가. 신규 McpImportEntry/Request/Result/ExportResponse. mcpApi.importServers/exportServers + useImportMcpServers/useExportMcpServers 추가.
- **mcp-server-wizard 재설계**: 586→760줄, DialogShell size=lg height=tall로 변환. shadcn Tabs variant=line 3탭(1·Basics / 2·Auth / 3·Tools). 자유 전환(step gate 제거). Header에 ProbeBadge — Probe needed → Probing → OK · N tools → Failed 라이프사이클. Basics 탭: registry quick-start grid + manual section(transport 라디오 3종, stdio면 args TagInput + envVars KV rows, http면 headers KV rows). Auth 탭: CredentialPicker + interpolation 도움말 박스 + Test connection 버튼. Tools 탭: 자동 probe(useEffect+ref guard) + per-tool Checkbox 카드 + parameters count badge + Re-probe.
- **mcp-import-dialog 신설**: DialogShell size=md height=fixed. Textarea(font-mono) + .json 업로드 + Overwrite Checkbox + 파싱 미리보기(서버 이름 리스트) + import 결과 카운터 칩(created/updated/skipped/errors).
- **mcp-servers/page.tsx 헤더**: Import + Export + New MCP server 3-button row. Export는 mutation으로 fetch 후 Blob+anchor click 다운로드(파일명 `mcp-servers-YYYY-MM-DD.json`).
- **mcp-tool-table 확장**: Schema 컬럼 추가(toggle로 row 아래 `<pre>JSON</pre>` expand). disabled 시 row dim + "disabled" 배지. last_seen_at 7일 이전 또는 null이면 "stale" 배지(`bg-status-warn/15`). React.Fragment로 row+schema 묶음.
- **mcp-server-detail-dialog 헤더**: actions에 is_system "System" 배지(`bg-status-info/15`) + StatusChip은 health_status를 우선 표시(없으면 server.status fallback).
- 함정/패턴: per-tool enable PATCH 훅이 wizard 단계에 없어 toDisable는 toast.info로 안내(detail 페이지에서 fine-tune). React Fragment에 두 TableRow를 한 키로 묶을 때 `<Fragment key>` 명시 필수.
- 검증: pnpm lint clean, pnpm build PASS (16 라우트, TS 4.3s).

## ── 2026-05-01 야간 — M-CHAT1 채팅 UI 고도화 ──

### 결정 사항 (불변)
- Sheet 사용 금지(이전에 모두 Dialog로 전환). 대신 **ChatRightRail** 도입 — inline split layout (portal 아님). 모드: subagent | tool-result | outline | none.
- 채팅 페이지 grid 재구성: [좌 사이드바 | 메시지+composer | 우 right rail (collapsible)]
- 한 번에 한 right rail 모드만 활성. 닫기 시 width 0 transition.
- HITL approval은 right rail 아니라 메시지 영역 내부 (banner 또는 하단)
- 9번 (Share) 제외. 별도 PR.

### 작업 순서
1. P0-4 (Right Rail 인프라 + Sub-agent) ← 다른 작업의 토대
2. P0-1, P0-2, P0-3 병렬
3. P1-5, P1-6, P1-7 병렬
4. P2-8, P2-10, P2-11, P2-12 병렬

### S-CHAT P0-4 저커버그 완료 (2026-05-01) — ChatRightRail 인프라 + Sub-agent 카드
- chat-right-rail.ts (Jotai atom) — RightRailState union: none/subagent/tool-result/outline. 한 번에 한 모드.
- ChatRightRail: 데스크톱은 inline split aside (width 0↔380px transition), 모바일은 fixed inset-0 overlay (Sheet 사용 X — 백드롭 button + 오른쪽 패널). page.tsx 외부 className으로 카드 외관(rounded-xl border bg-card shadow-sm) 주입 → 기존 두 카드와 시각적 일관성.
- subagent-panel-content.tsx: Agent / Input / Output 섹션. Output은 placeholder ("will appear as it streams") — 후속에서 tool_call result 구독 보강.
- sub-agent-ui.tsx: makeAssistantToolUI({toolName: 'task'}). deepagents 표준. agent_name/subagent_type, input/prompt/description fallback. phase별 status-warn(running) / status-success(complete) / status-danger(incomplete) 색. 카드 클릭 → setRail({mode:'subagent', subagent:{toolCallId,agentName,input}}).
- 함정: makeAssistantToolUI의 inline render에서 useSetAtom 직접 호출 시 react-hooks/rules-of-hooks 위반(함수명 'render' 소문자). → SubAgentCard 컴포넌트로 분리하고 render는 단순 위임.
- 도구 이름 확정: backend executor.py L505 "task 도구의 subagent_type" — deepagents 표준 task 도구. agent_name 키 외에도 subagent_type fallback 처리.
- 검증: pnpm lint clean + pnpm build PASS (16 라우트, 4.3s).

### S-CHAT P0-2 + P1-6 저커버그 완료 (2026-05-01)
- P0-2 conversation 검색: SearchInput을 conversation-list 헤더 아래 추가. searchQuery state로 client-side filter (`title.toLowerCase().includes(query)`); 검색 중에는 핀/일반 그룹 통합, 비어있으면 기존 "pinned 우선" 정렬 유지. 결과 0건이면 i18n `searchEmpty` 메시지 (기존 `empty`와 분리).
- P1-6 Mermaid: `pnpm add mermaid` (11.14.0). `mermaid-diagram.tsx` 신규 — `mermaid.initialize` 모듈-스코프 1회만(initialized flag), `useEffect`에서 `mermaid.render(id, code)` → SVG state. `useId()` 콜론 sanitize 필수 (`/[^a-zA-Z0-9-]/g → -`). `markdown-content.tsx`에서 `lazy(() => import("./mermaid-diagram"))`로 dynamic import → 메인 번들 보호 (mermaid는 별도 chunk). `MarkdownContent`에 `isStreaming?: boolean` prop 추가; 스트리밍 중에는 `language === "mermaid"`여도 raw CodeBlock fallback (불완전 mermaid 파싱 회피). 에러/로딩 시 raw code blocks fallback. `useMemo`로 components 생성 — isStreaming 변경 시에만 재계산.
- 검증: pnpm lint clean + pnpm build PASS (16 라우트, 5.5s). mermaid는 `.next/static/chunks/`에 별도 청크로 분리됨 확인.

### S-CHAT P2-10+11+12 저커버그 완료 (2026-05-01) — Skill row + ToolResult/Outline panels
- agent-skills-row.tsx: Agent.skills 배열을 chip row로 렌더 (BookOpenIcon + bg-primary-strong/10 chip). 0개면 row 자체 렌더 안함. chat page header 바로 아래(thread 위)에 배치.
- chat-right-rail.ts: ToolResultPayload에 args/result/status 추가, OutlinePayload에 content 추가 (parent에서 setRail 호출 시 전체 페이로드 전달 — 패널이 self-contained하게 렌더).
- tool-result-panel-content.tsx: tool name + StatusBadge(running/complete/incomplete) + Args/Result 섹션. ResultRenderer는 string→{이미지URL→ChatImage / URL→링크 / JSON→pretty pre / 일반텍스트→pre}, object→JSON pretty.
- 함정: react-hooks/error-boundaries 룰이 try/catch 안에서 JSX 생성을 금지함. → safeParseJson 헬퍼로 분리(try 내부는 parsed value 반환, JSX는 try 밖에서 렌더).
- outline-panel-content.tsx: markdown content에서 ATX 헤딩(# ~ ######) 추출. fenced code block(```/~~~) 안 라인은 스킵. 클릭 시 window CustomEvent('moldy:outline-jump') dispatch — 실제 scroll target은 P0-1 작업자가 후속.
- outline-trigger.tsx: 재사용 가능한 트리거(icon/compact 두 variant). MessageMetaRow 작업자가 import해서 사용. 본 작업에서는 컴포넌트만 신설, assistant-thread.tsx는 손대지 않음.
- generic-tool-ui.tsx: 헤더 row를 button → div로 재구성 후 (1) toolName 클릭 (확장 토글) (2) PanelRightOpenIcon (right rail로) (3) ChevronDown (기존 토글) 3개 액션 분리. toolCallId가 있을 때만 expand 버튼 표시. makeAssistantToolUI render에서 toolCallId 추가 전달.
- 검증: pnpm lint clean(내 코드), pnpm build PASS 16 routes 5.8s. use-approval-deadline.ts의 lint 에러는 HITL worker 영역이라 손대지 않음(pre-existing).

[2026-05-01 jensen P1-5 HITL Countdown + Auto-Extend]
- 백엔드 변경 없음 (client-side deadline 관리 결정). DB 스키마/엔드포인트 추가 안 함. interrupt 페이로드에 timeout_seconds/approval_id가 있다면 활용하도록 ApprovalArgs에 옵션 필드만 추가.
- frontend/src/lib/hooks/use-approval-deadline.ts 신설: deadlineRef + 1초 setInterval tick, 60초 이하 isUrgent, 30s cooldown으로 +60s extend, 만료 시 onExpire 1회. React 19 룰(react-hooks/purity) 회피: Date.now()는 effect 내부에서만 호출(useRef 초기값 X), set-state-in-effect 회피: setState는 setTimeout(tick, 0) 콜백 안에서 호출.
- approval-card.tsx 확장: 헤더 우측에 ClockIcon + MM:SS 카운트다운 배지(urgent면 animate-pulse + bg-status-warn). reject reason / edit args textarea의 onChange/onFocus에 extend() 연결. 만료 시 handleDecision('rejected', { reasonOverride: '시간 초과로 자동 거부' }).
- 다른 HITL 도구(user-input-ui, clarifying-question-ui)는 후속 PR로 분리 — 동일 패턴(useApprovalDeadline + onInteract prop)으로 적용 가능. 다중 approval 페이지네이션은 현재 시스템에서 한 번에 하나만 interrupt 처리하는 구조라 미적용.
- 검증: pnpm lint clean (이전 jensen 노트의 use-approval-deadline pre-existing lint 에러 = 본 작업으로 해소), pnpm build PASS 5.5s.

[2026-05-01 zuckerberg P0-1 메시지 편집·재생성·피드백 + P1-7 파일 첨부]
- assistant-ui 빌트인 primitive 활용으로 코드량 최소화: ActionBarPrimitive.Edit/Reload/FeedbackPositive/FeedbackNegative + ThreadPrimitive.Messages.UserEditComposer + ComposerPrimitive.AddAttachment/Attachments + AttachmentPrimitive.Root/Name/Remove. 직접 hover 로직 X.
- useChatRuntime에 onEdit/onReload/adapters.feedback/adapters.attachments 추가. onEdit는 새 user 메시지로 재스트리밍(LangGraph 분기 미지원이라 PoC 트레이드오프). onReload는 마지막 user 메시지를 다시 send. attachments는 onNew의 AppendMessage.attachments에서 id 추출 → streamChat options.attachmentIds → backend POST body.attachments.
- 백엔드: m27 message_feedbacks(uniq user+message), m28 message_attachments(orphan→링크). 라우터 POST/DELETE /api/messages/{id}/feedback (upsert toggle), POST /api/uploads (multipart, 20MiB cap, image/text/pdf만), GET /api/uploads/{id}. MessageResponse에 feedback + attachments 필드 추가. message_id는 LangGraph checkpoint id 문자열(FK 없음 — messages 테이블 자체가 없음).
- chat_service.list_messages_from_checkpointer가 user_id를 받아 feedback/attachments 동시 hydrate. 매 list 호출마다 2개 추가 쿼리(conversation 단위, 메시지 ID dict 매칭).
- attachment-adapter.ts: Pending(local UUID, requires-action) → send 시 uploadFile() → Complete(서버 upload id). assistant-ui가 send 시 자동 호출. content는 [attachment: name](url) markdown 링크.
- feedback-adapter.ts: useChatFeedbackAdapter 훅. submit 콜백에서 getActiveRating(messageId)으로 현재 상태 lookup → 같으면 DELETE, 다르면 POST. metadata.submittedFeedback 동기화는 convertMessage에서 message.feedback → metadata.submittedFeedback 매핑으로 처리(서버 → UI 반영).
- P0-3 (Composer Model 토글): 사용자 지시로 SKIP. 백엔드 messages POST에 model_id override 추가 안 함. composer dropdown 신설 안 함.
- P2-8 (Virtuoso 가상화): assistant-ui ThreadPrimitive.Messages가 자체 가상화를 제공하지 않으나, MessageByIndexProvider/edit composer/auto-scroll/streaming reorder 등이 inner에 깊이 통합되어 있어 react-virtuoso로 외부 교체 시 회귀 위험 큼. SKIP, 후속 PR로 분리. 마일스톤 권고: (1) ThreadPrimitive.Messages 자체에 virtualize prop 요청 issue (2) 임계 N(예: 100메시지) 초과 시 react-virtuoso fork 도입.
- 검증: backend ruff clean + pytest 688 passed (회귀 0), frontend pnpm lint clean + pnpm build PASS 5.6s 16 routes. 신규 테스트: test_feedback_router.py(2), test_uploads_router.py(2).
- 파일 경계 준수: conversation-list/markdown-content/approval-card/right-rail/tool-ui 미수정.

### M-CHAT1 핫픽스 (2026-05-01)
- 사용자 보고: 채팅 메시지가 쌓이지 않고 갱신됨
- 핫픽스: use-chat-runtime.ts의 useExternalStoreRuntime에 onEdit/onReload 전달 제거. assistant-thread.tsx에서 EditButton/RegenerateButton 호출 제거 (함수 정의는 보존).
- Copy + Feedback + 첨부 + 대화검색 + Mermaid + Right Rail + Sub-agent + Skill row + HITL countdown 기능은 그대로 유지
- 추후 M-CHAT1b에서 정식 복원 (LangGraph 분기 또는 새 user turn stamp 패턴)

[2026-05-01 zuckerberg M-CHAT1b LangGraph thread fork + BranchPicker]
- LangGraph CheckpointTuple 구조: NamedTuple(config, checkpoint, metadata, parent_config, pending_writes). config["configurable"]["checkpoint_id"]가 fork point. parent_config가 부모 체크포인트 추적 키. ainvoke({...}, config={"configurable": {"thread_id": tid, "checkpoint_id": cid}})로 호출하면 그 시점에서 새 branch 형성.
- 분기 트리 빌드 알고리즘: 모든 leaf 체크포인트(다른 체크포인트의 parent가 아닌 것)의 messages 리스트를 순회 → 같은 message id = 공유 노드, 같은 parent_id에 다른 message id = sibling. langchain BaseMessage.id가 stable한 점이 핵심 — 같은 invoke의 메시지는 동일 id 유지.
- assistant-ui branch wiring 옵션 3가지 평가: (1) ExternalStoreAdapter.messageRepository = ExportedMessageRepository (정식 트리), (2) onEdit/onReload + setMessages로 분기 (반평면), (3) 커스텀 BranchPicker (메타데이터 기반). PoC 시간 압박으로 (3) 채택 — convertMessage에서 message.siblings → metadata.custom.branches로 노출, 커스텀 BranchPicker가 useAssistantState로 읽고 conversationsApi.switchBranch + queryClient.invalidateQueries 호출. 정식 (1)로의 마이그레이션은 후속 PR(per-sibling checkpoint id 매핑 필요).
- 회귀 0: GET /messages 응답이 list → MessagesEnvelope({messages, active_tip_message_id, active_checkpoint_id})로 변경됐지만 conversationsApi.messages가 envelope.messages를 unwrap해서 기존 호출자 영향 없음.

[2026-05-01 zuckerberg M-CHAT1b HOTFIX active branch chain 표시 + sibling switch]
- 버그: _build_tree_from_checkpoints가 모든 leaf의 messages를 한 list로 DFS-emit → 편집 후 화면에 [old-user, old-ai, new-user, new-ai] 4개 모두 표시(기대: new 2개만). switch 클릭은 활성 branch의 같은 checkpoint를 보내 무반응.
- 수정: nodes는 active leaf의 root→leaf chain만 emit. 다른 leaf의 같은 idx 메시지는 BranchSibling(message_id, checkpoint_id)로 branches_by_message[active_mid]에 sibling 후보로 채움. active_checkpoint_id 인자 → Conversation.active_branch_checkpoint_id로 결정 (없으면 newest leaf fallback).
- 스키마: MessageResponse에 sibling_checkpoint_ids 필드 추가 (siblings와 동일 순서). 프론트는 BranchPicker가 currentIdx ± 1의 checkpoint_id로 switchBranch 호출 → invalidateQueries로 refetch.
- edit/regenerate 후 active_branch_checkpoint_id를 None으로 클리어 → 새 leaf가 자동으로 newest fallback에 의해 활성. 사용자가 명시적으로 ◀ 누르기 전엔 항상 최신 branch가 보이는 자연스러운 UX.

[2026-05-01 zuckerberg M-CHAT1b HOTFIX2 sibling order + index + flicker]
- 버그: (B1) edit/regenerate 동안 옛 메시지 + 스트리밍 새 메시지가 같이 보이다가 refetch 후에야 정리됨 = 깜빡임. (B2/B3) 새 분기 만들면 active=newest sibling인데 BranchPicker가 indexOf로 1/N(◀ 비활성) 표시 — 사실은 N/N(◀ 활성, ▶ 비활성)이 맞음. (B4) 3+ sibling 시 ◀▶가 최근 2개만 토글.
- 백엔드 수정: thread_branch_service에서 siblings를 checkpoint_id ASC(=oldest→newest, LangGraph checkpoint id가 시간순 sortable)로 정렬. active node에 branch_index/branch_total 직접 stamp → MessageResponse 두 필드 추가. 프론트가 indexOf로 위치 추정 안 해도 됨 → race condition 제거.
- 프론트엔드 수정: Message 타입 + convertMessage가 branch_index/branch_total 메타 노출. BranchPicker가 indexOf 제거하고 backend index 직접 사용. use-chat-runtime이 useQueryClient로 onEdit/onReload 시작 시점에 messages 캐시 truncate(편집된 message idx부터) → 스트리밍 동안 [잘려나간 chain] + 새 streaming만 보임, 깜빡임 0. onStreamEnd의 invalidateQueries가 정확한 active branch chain으로 최종 sync.
- 검증: backend 700 passed (회귀 0, +1 새 3-sibling 테스트), ruff clean, frontend lint clean + build PASS 16 routes.

[2026-05-01 zuckerberg M-CHAT1b HOTFIX3 regenerate user 중복 fix]
- 버그(B5): regenerate 후 화면이 [user, user, ai_new] — user 메시지가 두 번 표시. 원인은 regenerate 라우터가 rewind 후에도 user content를 messages_history로 재주입 → LangGraph add_messages reducer가 user를 또 append. (B6): user 두 번 들어가니 sibling 트리가 깨져 BranchPicker 미표시.
- 수정: regenerate는 rewind_to_checkpoint_before_message(target_assistant_id)로 "user는 있고 assistant는 없는" checkpoint로 fork → execute_agent_stream(cfg, [])로 빈 history 전달. executor가 lc_messages 비면 stream_input=None, streaming.py가 None을 그대로 agent.astream(None, config)로 통과 → LangGraph time-travel resume으로 새 assistant만 생성. 결과 [..., user, ai_new] (user 1회), ai_new는 기존 ai와 같은 user 부모를 공유 → 기존 thread_branch_service가 자동으로 sibling으로 묶음 + branch_index/total stamp.
- 검증: backend 701 passed (회귀 0, +2 신규: test_regenerate_does_not_duplicate_user_message + test_regenerate_targeted_assistant_uses_correct_checkpoint), ruff clean, frontend lint clean + build PASS.

[2026-05-01 zuckerberg SIMPLIFY P0+P1 일괄 fix]
- P0-A/B/C: SSE/executor/stream-runner 3종이 모두 같은 (prep → stream → try/except → cleanup) 패턴 → 각 레이어에 헬퍼 1개씩 추출(_sse_handler, _run_agent_stream + _USE_PREPPED_LC_MESSAGES sentinel, _runStream). 4개 endpoint × 3 layers = 12 함수의 중복 약 -180 LOC.
- P0-D: build_message_tree가 GET /messages 한 요청에 router + chat_service에서 2회 호출 → tree 빌드 1회 후 chat_service에 인자로 주입. _build_tree_from_checkpoints는 leaf×messages마다 ancestor chain을 매번 다시 walk → leaf별 chain 1회 빌드 후 _checkpoint_for_message_in_chain로 lookup. O(L×D×M) → O(L×D) + O(L×M).
- 패턴: 같은 try/except 보일러플레이트가 3+ 곳 반복되면 lazy factory(callable) 헬퍼로 추출. AsyncGenerator는 generator factory를 람다로 감싸 _sse_handler에 넘기는 패턴이 가장 깔끔.
