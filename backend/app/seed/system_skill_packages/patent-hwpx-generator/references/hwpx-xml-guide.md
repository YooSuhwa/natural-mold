# HWPX XML Notes

HWPX files are ZIP packages. This skill keeps the template package structure and replaces visible text in `Contents/section0.xml` plus the modified date in `Contents/content.hpf`.

The generator preserves ZIP entry metadata and only updates text-bearing XML parts required for the sample patent-style report.
